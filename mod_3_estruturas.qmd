# Problemas e estruturas das técnicas multivariadas {#sec-estruturas}

Os Módulos 1 e 2 estabeleceram a base matemática, geométrica, probabilística e inferencial utilizada na Análise Multivariada. Vetores e matrizes fornecem a representação dos dados; distâncias, produtos internos e projeções descrevem sua geometria; autovetores e vetores singulares identificam direções relevantes, enquanto autovalores e valores singulares quantificam magnitudes associadas a essas direções; matrizes de covariâncias e correlações descrevem a estrutura de variabilidade conjunta e de associação linear; e os modelos probabilísticos e inferenciais permitem formular conclusões sobre populações multivariadas.

Neste módulo, esses conhecimentos serão combinados para formular as principais técnicas multivariadas. Cada técnica será tratada como uma construção que articula o problema estatístico aos objetos matemáticos, probabilísticos e inferenciais já desenvolvidos, tornando explícitas as ligações entre esses fundamentos e a solução obtida. O ponto de partida passa a ser o problema estatístico. Redução de dimensionalidade, representação de estruturas latentes, formação de grupos, análise de associações em tabelas de contingência, comparação de vetores de médias e discriminação de grupos exigem formulações distintas [@johnson2007].

Uma mesma estrutura matemática pode aparecer em técnicas diferentes. A decomposição espectral pode identificar direções de maior variabilidade, fornecer uma solução inicial para cargas fatoriais no método de fatores principais ou participar da determinação de direções de separação entre grupos. O significado estatístico depende do objeto analisado e do critério utilizado.

A formulação das técnicas deste módulo será organizada pelos elementos apresentados na @tbl-estrutura-formulacao-tecnicas.

| Elemento | Função na formulação |
|---|---|
| Problema estatístico | Define o objetivo da análise |
| Estrutura dos dados | Estabelece a forma como a informação está organizada |
| Objeto matemático | Representa a informação relevante para a técnica |
| Critério ou modelo | Determina a propriedade otimizada, decomposta ou modelada |
| Solução | Produz direções, coordenadas, grupos, parâmetros ou funções |
| Interpretação | Atribui significado estatístico e geométrico à solução |

: Elementos utilizados na formulação das técnicas multivariadas. {#tbl-estrutura-formulacao-tecnicas}

O objetivo deste capítulo é organizar os principais problemas da Análise Multivariada, identificar as estruturas compartilhadas pelas técnicas que serão desenvolvidas nos capítulos seguintes e explicitar como os fundamentos dos Módulos 1 e 2 passam a compor sua formulação.

## Do problema estatístico à técnica multivariada

A presença de várias variáveis em um conjunto de dados caracteriza o contexto multivariado, mas não determina a técnica a ser utilizada. A escolha depende da estrutura que será analisada e do objetivo estabelecido.

Considere a matriz de dados $\mathbf X$ definida na @def-matriz-dados. Quando suas colunas correspondem a variáveis quantitativas, cada observação pode ser representada geometricamente pelas respectivas coordenadas em $\mathbb R^p$, conforme discutido na @sec-natureza-variaveis. Variáveis categóricas não devem ser incorporadas automaticamente a essa geometria apenas pela atribuição de códigos numéricos; técnicas que utilizam categorias ou dados mistos exigem representações compatíveis com a natureza das variáveis.

A mesma matriz de dados quantitativos pode ser utilizada para construir combinações lineares que concentrem variabilidade, identificar grupos de observações semelhantes ou, quando os grupos são conhecidos, determinar direções que os separem. O conjunto de dados permanece o mesmo, enquanto o objeto de interesse e o critério de solução se modificam.

A @tbl-problemas-tecnicas reúne as principais correspondências que serão desenvolvidas neste módulo.

| Problema estatístico | Estrutura ou objeto central | Critério ou modelo | Técnica |
|---|---|---|---|
| Representar a variabilidade em menor dimensão | $\boldsymbol{\Sigma}$, $\mathbf S$, $\boldsymbol{\mathcal R}$, $\mathbf R$ ou matriz de dados centralizada | Maximização da variância e aproximação de posto reduzido | Análise de Componentes Principais |
| Explicar associações por dimensões não observadas | Matriz de covariâncias ou correlações | Modelo de fatores comuns | Análise Fatorial |
| Formar grupos de objetos semelhantes | Matriz de dados ou de dissimilaridades | Critérios de proximidade, dispersão ou ligação; modelos probabilísticos de mistura | Análise de Agrupamentos |
| Representar associações entre categorias | Tabela de contingência, massas e perfis | Geometria qui-quadrado, SVD e decomposição da inércia | Análise de Correspondência |
| Comparar conjuntamente várias respostas | Matrizes de hipótese e erro | Raízes características e critérios multivariados de teste | Análise de Variância Multivariada |
| Separar grupos conhecidos e classificar observações | Médias, covariâncias, probabilidades de classe e dispersões de grupos | Regra de Bayes, LDA/QDA ou critério de Fisher | Análise Discriminante |

: Correspondência entre problemas, estruturas e técnicas multivariadas. {#tbl-problemas-tecnicas}

Os objetos utilizados nessa tabela já começaram a ser construídos nos módulos anteriores. A matriz de dados definida na @def-matriz-dados permite trabalhar diretamente com observações e variáveis. A matriz de covariâncias populacional $\boldsymbol{\Sigma}$, definida na @def-matriz-covariancias-populacional, e sua correspondente observada $\mathbf S$, definida na @def-matriz-covariancias-observada, descrevem a variabilidade conjunta. A matriz de correlações observada $\mathbf R$, definida na @def-matriz-correlacoes-observada, corresponde à estrutura obtida após a padronização marginal das variáveis.

A escolha entre covariâncias e correlações não altera o objetivo estatístico da técnica, mas modifica a representação da variabilidade utilizada na formulação e pode, consequentemente, modificar a solução. A matriz de covariâncias preserva as escalas originais, enquanto a matriz de correlações atribui variância unitária às variáveis. Essa diferença será retomada no @sec-acp ao formular a Análise de Componentes Principais (ACP) a partir de $\mathbf S$ ou $\mathbf R$. A influência da escala também será retomada no @sec-agrupamentos, pois a padronização das variáveis pode modificar as distâncias entre os objetos e, consequentemente, a estrutura dos grupos obtidos.

Outras técnicas utilizam estruturas diferentes. A @sec-agrupamentos pode partir de dados multivariados ou de dissimilaridades entre objetos e admite diferentes definições matemáticas de grupo. A @sec-correspondencia parte de frequências organizadas em uma tabela de contingência e constrói massas, perfis e uma geometria ponderada. A @sec-manova utiliza diretamente a estrutura do modelo linear multivariado definido na @def-modelo-linear-multivariado. A @sec-discriminante utiliza informações sobre grupos previamente conhecidos e articula formulações probabilísticas de classificação com a formulação geométrica de Fisher.

A estrutura de entrada delimita a informação disponível para a técnica. Uma matriz de covariâncias resume relações de segunda ordem entre variáveis. Uma matriz de dissimilaridades representa relações entre pares de objetos. Uma tabela de contingência registra frequências conjuntas entre categorias. A formulação deve preservar essa diferença.

## Classes de problemas multivariados

As técnicas deste módulo podem ser organizadas de acordo com o problema estatístico que resolvem. Essa classificação estabelece a função de cada método antes de sua formulação matemática.

### Redução de dimensionalidade

A redução de dimensionalidade substitui uma representação de dimensão elevada por outra de menor dimensão, preservando uma característica definida da informação original.

Na @sec-acp, o critério é a variabilidade. As novas dimensões são combinações lineares escolhidas para concentrar sucessivamente a variância. A formulação utiliza as projeções de @def-projecao-ortogonal, a decomposição espectral sintetizada na @tbl-sintese-espectral e a decomposição em valores singulares sintetizada na @tbl-sintese-svd.

Representações de menor dimensão também aparecem em outras técnicas, mas associadas a problemas estatísticos distintos. Na @sec-correspondencia, as dimensões representam associações entre perfis de linhas e colunas de uma tabela de contingência. A propriedade que se procura representar faz parte da definição do problema.

### Representação de estruturas latentes

A @sec-fatorial procura representar as associações entre variáveis observadas por meio de um número menor de fatores não observados.

Nesse caso, a matriz de covariâncias passa a ser objeto de um modelo. A formulação separa a parcela de variabilidade associada aos fatores comuns da parcela específica de cada variável.

Essa estrutura distingue a @sec-fatorial da @sec-acp. A ACP procura uma representação da variabilidade observada, enquanto a Análise Fatorial especifica um modelo para explicar a covariação entre as variáveis.

### Formação de grupos

A Análise de Agrupamentos organiza objetos em grupos sem utilizar classes conhecidas previamente. A solução depende da representação dos objetos, da medida de proximidade e do critério que define a organização procurada.

A distância euclidiana definida na @def-distancia-euclidiana fornece uma medida de proximidade já estabelecida no Módulo 1. Na @sec-agrupamentos, uma dissimilaridade é formalizada na @def-dissimilaridade-agrupamento. As $K$-médias produzem uma partição, objeto definido na @def-particao-agrupamento, enquanto os métodos hierárquicos produzem uma sequência de partições aninhadas, formalizada na @def-hierarquia-agrupamentos. Uma terceira formulação define grupos probabilisticamente por meio do modelo da @def-modelo-mistura-agrupamento.

Essas estruturas não são equivalentes. Sem um modelo probabilístico explícito, os grupos obtidos representam a solução do critério adotado e não correspondem automaticamente a populações distintas.

### Representação de associações em tabelas de contingência

A Análise de Correspondência parte de uma tabela de frequências e representa os desvios em relação à independência entre suas categorias.

Na @sec-correspondencia, as frequências marginais originam as massas definidas na @def-massas-correspondencia e os perfis definidos na @def-perfis-correspondencia. A comparação entre perfis utiliza a distância qui-quadrado da @def-distancia-qui-quadrado-ca, e os desvios padronizados da independência são organizados pela matriz definida na @def-matriz-desvios-padronizados-ca.

A @sec-ca-svd mostra como a SVD dessa matriz decompõe a inércia e produz as dimensões da representação. As coordenadas padrão e principais são formalizadas, respectivamente, na @def-coordenadas-padrao-ca e na @def-coordenadas-principais-ca.

### Comparação de vetores de médias

Na MANOVA, a comparação de vetores de médias constitui um caso particular de um problema mais geral: testar hipóteses sobre funções estimáveis dos parâmetros de um modelo linear multivariado com várias respostas.

O modelo definido na @def-modelo-linear-multivariado e a hipótese linear geral da @def-hipotese-linear-geral-multivariada fornecem a estrutura necessária. A @sec-manova acrescenta as matrizes de hipótese e erro e conduz ao problema de autovalores generalizados apresentado em @eq-autovalor-generalizado-manova. Suas raízes são formalizadas na @def-raizes-caracteristicas-manova e utilizadas na construção dos quatro critérios reunidos na @sec-manova-criterios.

A análise conjunta das respostas distingue a MANOVA de uma coleção de análises univariadas separadas.

### Discriminação e classificação

A Análise Discriminante utiliza grupos conhecidos durante a construção da solução. Seu objetivo pode ser representar direções de separação entre esses grupos ou construir regras de classificação para novas observações.

Na formulação probabilística, distribuições condicionais e probabilidades *a priori* conduzem à regra de Bayes apresentada em @eq-regra-bayes-classificacao-discriminante. A distribuição normal multivariada definida na @def-normal-multivariada e a distância de Mahalanobis definida na @def-distancia-mahalanobis fornecem a base para as formulações da @sec-discriminante-lda e da @sec-discriminante-qda.

A formulação geométrica da @sec-discriminante-fisher compara a dispersão entre grupos à dispersão intragrupos. O critério amostral é definido na @def-criterio-fisher-discriminante e conduz ao problema de autovalores generalizados de @eq-autovalor-generalizado-fisher.

A Análise Discriminante e a MANOVA compartilham estruturas de dispersão e problemas espectrais relacionados, mas possuem objetivos diferentes [@huberty2006manova]. Essa relação é explicitada posteriormente em @eq-correspondencia-manova-fisher.

## Diferentes formas de classificar as técnicas

A classificação pelo problema estatístico será a principal organização adotada neste módulo. Outras classificações permitem comparar diferentes características das técnicas.

### Interdependência e dependência

Nas técnicas de interdependência, a análise não estabelece previamente uma variável resposta que será explicada pelas demais. ACP, Análise Fatorial, Análise de Agrupamentos e Análise de Correspondência são exemplos.

Nas técnicas de dependência, alguma estrutura previamente definida orienta a análise. Na MANOVA, as respostas e os efeitos são determinados pelo delineamento. Na Análise Discriminante, os grupos são conhecidos durante a construção da solução.

### Exploratórias, descritivas, preditivas e inferenciais

Uma técnica exploratória identifica padrões ou estruturas nos dados. Uma técnica descritiva resume uma configuração observada. Uma técnica preditiva produz uma regra aplicável a novas observações. Uma técnica inferencial utiliza a variabilidade amostral para formular conclusões sobre parâmetros ou populações.

Essas finalidades podem coexistir. A ACP é predominantemente exploratória e descritiva em muitas aplicações, embora exista teoria inferencial para componentes populacionais. A Análise Discriminante descreve a separação entre grupos e também pode produzir regras de classificação. A MANOVA possui finalidade principalmente inferencial.

### Supervisionadas e não supervisionadas

A distinção entre métodos supervisionados e não supervisionados constitui uma classificação complementar e não uma taxonomia igualmente natural para todas as técnicas multivariadas.

Métodos supervisionados utilizam uma informação externa que orienta a construção da solução. Os rótulos dos grupos desempenham esse papel diretamente na Análise Discriminante. Na MANOVA, há uma estrutura previamente especificada pelo delineamento, que determina os efeitos avaliados, embora a terminologia supervisionada seja menos usual para essa técnica.

Métodos não supervisionados constroem a estrutura diretamente a partir dos dados, sem utilizar classes ou respostas previamente especificadas. ACP e Análise de Agrupamentos são exemplos.

Análise Fatorial e Análise de Correspondência são caracterizadas de forma mais adequada pelo problema estatístico e pela estrutura que procuram representar do que pela oposição entre supervisão e não supervisão.

A @tbl-classificacoes-tecnicas resume essas classificações.

| Técnica | Problema predominante | Relação entre variáveis | Finalidade frequente | Estrutura de supervisão |
|---|---|---|---|---|
| ACP | Redução de dimensionalidade | Interdependência | Exploratória e descritiva | Não supervisionada |
| Análise Fatorial | Estrutura latente | Interdependência | Exploratória; inferencial sob formulações probabilísticas específicas | Sem resposta externa na formulação exploratória |
| Análise de Agrupamentos | Formação de grupos | Interdependência | Exploratória e descritiva | Não supervisionada |
| Análise de Correspondência | Associação em tabelas | Interdependência | Exploratória e descritiva | Não supervisionada |
| MANOVA | Comparação de vetores de médias | Dependência | Inferencial | Delineamento conhecido |
| Análise Discriminante | Discriminação e classificação | Dependência | Descritiva e preditiva | Grupos conhecidos |

: Classificações complementares das técnicas estudadas no Módulo 3. {#tbl-classificacoes-tecnicas}

A classificação pelo problema estatístico permanece mais diretamente ligada à formulação das técnicas. Por essa razão, os capítulos seguintes serão organizados pelo problema, pelo objeto matemático e pelo critério ou modelo utilizado.

## Estruturas matemáticas recorrentes {#sec-estruturas-matematicas-recorrentes}

As técnicas deste módulo reutilizam um conjunto relativamente pequeno de estruturas desenvolvidas anteriormente. O objetivo desta seção é estabelecer as conexões necessárias, sem repetir definições e demonstrações dos Módulos 1 e 2.

### Combinações lineares e projeções

As combinações lineares definidas na @def-combinacao-linear constroem novas variáveis e novas direções a partir das variáveis originais. Para um vetor aleatório $\boldsymbol{\mathsf X}$, sua forma é

$$
Y
=
\mathbf a^\top
\boldsymbol{\mathsf X}.
$$

O significado de $\mathbf a$ depende do critério da técnica. Na @sec-acp, seus coeficientes definem direções de variabilidade. Na @sec-manova, combinações lineares das respostas são utilizadas para comparar hipótese e erro. Na @sec-discriminante-fisher, os coeficientes definem direções de separação entre grupos.

As projeções ortogonais definidas na @def-projecao-ortogonal permitem representar observações em subespaços. Essa geometria é utilizada no @sec-acp para interpretar a projeção sobre o subespaço principal e na @sec-discriminante-representacao para representar as observações no subespaço discriminante.

### Formas quadráticas e problemas espectrais

As formas quadráticas definidas na @def-forma-quadratica aparecem na variância de combinações lineares, em distâncias ponderadas e em diferentes medidas de dispersão. Quando o critério depende da direção escolhida, surgem problemas de otimização espectral.

O quociente de Rayleigh definido na @def-quociente-rayleigh relaciona a otimização de uma forma quadrática aos autovalores de uma matriz simétrica. O resultado do @thm-extremos-quociente-rayleigh é aplicado no @sec-acp à matriz de covariâncias ou de correlações para formular as componentes principais.

Quando duas formas quadráticas são comparadas, o quociente de Rayleigh generalizado da @def-quociente-rayleigh-generalizado conduz ao problema de autovalores generalizados descrito no @thm-extremos-rayleigh-generalizado. Essa estrutura aparece concretamente em @eq-autovalor-generalizado-manova e @eq-autovalor-generalizado-fisher.

Quando a matriz do denominador é positiva definida, a ideia de branqueamento já desenvolvida em @eq-transformacao-branqueamento-ponto permite transformar a métrica correspondente em uma geometria euclidiana. Essa construção reaparece no problema simétrico da MANOVA em @eq-problema-simetrico-manova e na matriz branqueada de Fisher em @eq-matriz-fisher-branqueada.

A decomposição espectral, sintetizada na @tbl-sintese-espectral, fornece as direções e magnitudes associadas a uma matriz simétrica. A interpretação dessas direções depende da matriz que foi construída e do problema estatístico que a originou.

### Otimização sob restrições e critérios de ajuste {#sec-estruturas-otimizacao}

Diversas técnicas multivariadas definem suas soluções por meio da maximização ou minimização de um critério. Em termos gerais, a solução pode ser caracterizada por um problema da forma

$$
\max_{\boldsymbol{\theta}}
f(\boldsymbol{\theta})
$$

ou

$$
\min_{\boldsymbol{\theta}}
Q(\boldsymbol{\theta}),
$$

eventualmente sujeito a restrições que delimitam o conjunto de soluções admissíveis.

Quando a função objetivo envolve uma forma quadrática e a restrição fixa a norma do vetor, os multiplicadores de Lagrange estabelecem a relação entre o problema de otimização e a equação de autovalores. A condição geral foi apresentada no @thm-multiplicadores-lagrange e a otimização sob restrição de norma foi ilustrada no @exm-lagrange-restricao-norma.

Essa estrutura aparece de formas distintas nas técnicas deste módulo. Na ACP, procura-se uma direção que maximize a variância sob uma restrição de normalização. Na Análise Discriminante de Fisher, maximiza-se a separação entre grupos relativamente à dispersão intragrupos. Nas $K$-médias, procura-se uma partição que minimize a dispersão intragrupos. Na Análise Fatorial, determinados métodos de estimação definem a solução pela minimização de resíduos entre estruturas de covariâncias ou pela maximização de uma função de verossimilhança.

O critério de otimização faz parte da definição da solução estatística. A obtenção de autovetores, centroides ou estimativas de parâmetros deve ser interpretada em relação à função que está sendo maximizada ou minimizada e às restrições que caracterizam o problema.

### SVD e representações de baixa dimensão

A decomposição em valores singulares relaciona os espaços de linhas e colunas de uma matriz e permite construir aproximações de posto reduzido. Suas propriedades foram sintetizadas na @tbl-sintese-svd.

No Módulo 3, a SVD aparece em contextos distintos. No @sec-acp, é aplicada à matriz de dados centralizada. Na @sec-correspondencia, é aplicada à matriz de desvios padronizados definida na @def-matriz-desvios-padronizados-ca, e a decomposição correspondente é desenvolvida na @sec-ca-svd.

O mecanismo algébrico é compartilhado, mas a matriz decomposta e o significado estatístico dos vetores e valores singulares são diferentes.

### Distâncias e métricas

Distâncias organizam relações de proximidade entre observações; quando uma técnica admite dissimilaridades mais gerais, elas serão especificadas no capítulo correspondente. A distância euclidiana definida na @def-distancia-euclidiana é central na formulação das $K$-médias e participa da construção de métodos de agrupamento. A @def-dissimilaridade-agrupamento amplia essa representação no @sec-agrupamentos.

Na @sec-correspondencia, a @def-distancia-qui-quadrado-ca estabelece uma geometria ponderada adequada aos perfis. A distância de Mahalanobis, definida na @def-distancia-mahalanobis, incorpora a estrutura de dispersão à geometria. A expressão em eixos espectrais apresentada em @eq-mahalanobis-eixos-espectrais mostra que o afastamento é ponderado pela variabilidade existente em cada direção e fornece parte da base geométrica das regras normais desenvolvidas na @sec-discriminante-lda e na @sec-discriminante-qda.

### Modelos lineares e probabilísticos

O modelo linear multivariado definido na @def-modelo-linear-multivariado organiza conjuntamente várias respostas, um delineamento e uma matriz de parâmetros. A hipótese linear geral da @def-hipotese-linear-geral-multivariada fornece a estrutura para testar combinações desses parâmetros. A @sec-manova utiliza diretamente esses resultados e acrescenta as matrizes de hipótese e erro, as raízes características e os critérios da @sec-manova-criterios.

Outras técnicas acrescentam estruturas probabilísticas específicas à sua formulação. Na @sec-fatorial, o modelo definido na @def-modelo-fatores-comuns especifica uma estrutura para a matriz de covariâncias, e a estimação por máxima verossimilhança acrescenta uma formulação probabilística baseada na normalidade multivariada. Na @sec-discriminante, distribuições condicionais aos grupos e probabilidades *a priori* são utilizadas na construção de regras probabilísticas de classificação. Na @sec-agrupamentos-definicoes-grupo, a @def-modelo-mistura-agrupamento apresenta uma definição probabilística alternativa de grupo.

Quando a normalidade multivariada é utilizada, sua definição e suas propriedades são as estabelecidas na @def-normal-multivariada. Essa hipótese deve ser distinguida das estruturas algébricas e geométricas utilizadas na formulação das técnicas: ela não constitui uma condição geral para construções baseadas em combinações lineares, formas quadráticas, projeções, SVD ou problemas espectrais.

Da mesma forma, quando uma técnica utilizar máxima verossimilhança ou comparação de modelos por verossimilhanças, serão retomadas a função de verossimilhança definida na @def-funcao-verossimilhanca e a razão de verossimilhanças definida na @def-razao-verossimilhancas. Os resultados inferenciais também preservarão a distinção entre resultados exatos e assintóticos estabelecida na @def-resultados-exatos-assintoticos.

A @tbl-estruturas-recorrentes-tecnicas reúne essas conexões.

| Estrutura desenvolvida anteriormente | Utilização no Módulo 3 | Técnicas diretamente relacionadas |
|---|---|---|
| Centralização e padronização | Definição da origem e da escala da representação dos dados | ACP, Agrupamentos |
| Combinações lineares | Construção de novas direções ou variáveis | ACP, MANOVA, Discriminante |
| Projeções | Representação em subespaços e direções | ACP, Discriminante |
| Formas quadráticas | Medida de variabilidade, distância, associação ou separação | ACP, Correspondência, MANOVA, Discriminante |
| Otimização sob restrições e critérios de ajuste | Definição de soluções por maximização ou minimização de um critério | ACP, Fatorial, Agrupamentos, Discriminante |
| Autovalores e autovetores | Decomposição espectral e determinação de direções; em métodos específicos, obtenção de soluções de extração | ACP; Fatorial em métodos específicos de extração |
| Autovalores e autovetores generalizados | Comparação entre formas quadráticas e determinação de direções extremas | MANOVA, Discriminante |
| SVD | Decomposição e representação de baixa dimensão | ACP, Correspondência |
| Massas, perfis e ponderações marginais | Construção da geometria de associações categóricas | Correspondência |
| Distâncias e métricas | Representação de proximidade, associação, dispersão ou classificação | Agrupamentos, Correspondência, MANOVA, Discriminante |
| Covariâncias e correlações | Representação da variabilidade conjunta e da associação linear | ACP, Fatorial, MANOVA, Discriminante |
| Dispersão dentro e entre grupos | Comparação da estrutura de grupos | Agrupamentos, MANOVA, Discriminante |
| Modelo linear multivariado | Representação conjunta de respostas e efeitos | MANOVA |
| Normal multivariada | Formulações probabilísticas e resultados inferenciais sob hipóteses específicas | Fatorial, Discriminante, MANOVA |
| Função e razão de verossimilhanças | Estimação e comparação sob formulações probabilísticas específicas | Fatorial por máxima verossimilhança; MANOVA na fundamentação probabilística de critérios específicos |

: Estruturas dos Módulos 1 e 2 reutilizadas na formulação das técnicas. {#tbl-estruturas-recorrentes-tecnicas}

## Mesmas estruturas, diferentes interpretações {#sec-estruturas-interpretacoes}

Uma operação matemática adquire significado estatístico a partir do objeto ao qual é aplicada.

No @sec-acp, a decomposição espectral da matriz de covariâncias ou correlações determina direções principais e as variâncias associadas a essas direções. No @sec-fatorial, o método de fatores principais aplica uma decomposição espectral a uma matriz reduzida, cuja diagonal contém comunalidades estimadas, para obter uma solução inicial para as cargas fatoriais. Um problema de autovalores generalizados envolvendo duas matrizes de dispersão, por sua vez, produz direções que comparam diferentes fontes de variabilidade.

A SVD apresenta a mesma característica. Aplicada à matriz de dados centralizada, participa da formulação da ACP. Aplicada aos desvios padronizados em relação à independência de uma tabela de contingência, produz os eixos da Análise de Correspondência.

A @tbl-mesma-operacao-significados resume essas diferenças.

| Operação matemática | Objeto analisado | Significado da solução |
|---|---|---|
| Decomposição espectral | Matriz de covariâncias ou correlações | Direções e variâncias principais |
| Decomposição espectral | Matriz reduzida da Análise Fatorial | Solução inicial para as cargas fatoriais |
| Autovalores generalizados | Dispersão entre e dentro dos grupos | Direções de separação |
| Autovalores generalizados | Matrizes de hipótese e erro | Dimensões associadas ao efeito multivariado |
| SVD | Matriz de dados centralizada | Direções, escores e representação de baixa dimensão |
| SVD | Desvios padronizados em relação à independência | Eixos de associação entre categorias |

: Exemplos de estruturas matemáticas com diferentes significados estatísticos. {#tbl-mesma-operacao-significados}

A interpretação de um autovalor, autovetor, valor singular, vetor singular ou coordenada exige a identificação da matriz de origem e do critério que definiu sua construção. A operação matemática utilizada, isoladamente, não identifica a técnica: procedimentos distintos podem empregar a mesma decomposição, otimização ou medida de distância e, ainda assim, resolver problemas estatísticos diferentes. A identificação do método exige considerar conjuntamente o objeto analisado, o critério ou modelo adotado e o significado atribuído à solução.

As fontes de não unicidade também devem ser distinguidas. Na ACP, a indeterminação de sinal e os efeitos da multiplicidade são desenvolvidos na @sec-acp-multiplicidade-singularidade. Na Análise Fatorial, a @prp-indeterminacao-rotacional-fatorial formaliza uma indeterminação adicional, associada à rotação dos fatores. Na Análise de Agrupamentos, a não unicidade pode decorrer da existência de diferentes soluções ótimas, como discutido na @sec-agrupamentos-existencia-nao-unicidade, ou de empates durante a construção de uma hierarquia, tratados na @sec-agrupamentos-empates-hierarquia. Na MANOVA, sinais e multiplicidades das direções são discutidos na @sec-manova-identificacao-direcoes. Essas situações possuem origens matemáticas distintas e não devem ser interpretadas como uma única forma de indeterminação.

## Relações entre as técnicas {#sec-relacoes-tecnicas-multivariadas}

Algumas técnicas apresentam estruturas matemáticas próximas, mas resolvem problemas diferentes. A @tbl-relacoes-tecnicas reúne relações que serão retomadas nos capítulos correspondentes.

| Técnicas relacionadas | Estrutura compartilhada | Diferença principal |
|---|---|---|
| ACP e Análise Fatorial | Covariâncias, correlações e decomposições de baixa dimensão | ACP representa a variabilidade total; Fatorial separa estrutura comum e variabilidade específica |
| Agrupamentos e Discriminante | Estrutura de grupos e medidas de separação | A Análise de Agrupamentos constrói grupos; a Análise Discriminante utiliza grupos conhecidos |
| MANOVA e Discriminante | Dispersão entre e dentro dos grupos | MANOVA testa efeitos; Discriminante representa separação e classifica |
| ACP e Correspondência | Decomposições de baixa dimensão | ACP utiliza a geometria de covariâncias; Correspondência utiliza perfis e geometria qui-quadrado |

: Relações conceituais entre técnicas do Módulo 3. {#tbl-relacoes-tecnicas}

A relação entre ACP e Análise Fatorial é desenvolvida no @sec-fatorial e sintetizada na @tbl-acp-fatorial-formulacao. As relações da Análise de Agrupamentos com outras técnicas são organizadas na @tbl-relacoes-agrupamentos-tecnicas. A conexão espectral entre MANOVA e a formulação de Fisher é explicitada em @eq-correspondencia-manova-fisher. A relação entre ACP e Análise de Correspondência é desenvolvida na @sec-correspondencia e sintetizada na @tbl-relacao-acp-ca.

Essas conexões mostram que a identificação da técnica depende do problema estatístico, do objeto analisado e do critério de solução. A operação matricial utilizada, isoladamente, não determina o método.

## Síntese

As técnicas multivariadas deste módulo utilizam os fundamentos construídos nos Módulos 1 e 2 para resolver problemas estatísticos distintos. A formulação de cada técnica combina uma estrutura de dados, um objeto matemático e um critério ou modelo.

A centralização e a padronização definem a origem e a escala da representação utilizada por determinadas técnicas. Combinações lineares e projeções produzem novas direções e representações. Formas quadráticas e problemas espectrais permitem estudar variabilidade e comparar diferentes fontes de dispersão. Na MANOVA e na formulação de Fisher, o branqueamento converte a métrica determinada pela matriz de referência em uma geometria euclidiana e conduz a problemas espectrais simétricos equivalentes.

Autovalores e autovetores definem diretamente a solução da ACP e aparecem na Análise Fatorial em métodos específicos de extração; o modelo fatorial, porém, é definido pela decomposição da covariância em uma parte comum e uma parte específica. A SVD fornece representações de baixa dimensão na ACP e, na @sec-ca-svd, decompõe a inércia da Análise de Correspondência. Na @sec-agrupamentos-definicoes-grupo, diferentes critérios geométricos e probabilísticos mostram que grupo não corresponde a um único objeto matemático. A @sec-manova-criterios constrói quatro critérios a partir das mesmas raízes características, enquanto a @sec-discriminante articula decisão probabilística, LDA/QDA e a geometria de Fisher.

As hipóteses probabilísticas devem ser distinguidas das estruturas algébricas e geométricas. Normalidade, máxima verossimilhança e resultados inferenciais serão utilizados somente quando fizerem parte da formulação específica da técnica.

Os capítulos seguintes utilizarão esses fundamentos sem reconstruí-los. O @sec-acp inicia esse percurso formulando a Análise de Componentes Principais a partir da variância de combinações lineares, do quociente de Rayleigh, da decomposição espectral, das projeções e da SVD.