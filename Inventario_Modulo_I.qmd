# Inventário consolidado do Módulo I

## Como utilizar este inventário nos próximos módulos

Este inventário registra os conteúdos já desenvolvidos no **Módulo I — Fundamentos Matemáticos e Geométricos da Análise Multivariada** e deve funcionar como referência para a construção e revisão dos Módulos II e III.

Há três situações diferentes que devem ser distinguidas.

| Situação | Conduta nos Módulos II e III |
|:---|:---|
| Conceito já definido e suficiente | **Referenciar**, sem redefinir |
| Conceito já apresentado matematicamente, mas que ganha interpretação estatística nova | **Retomar brevemente e desenvolver a interpretação nova** |
| Conceito apenas antecipado ou ainda não definido | **Definir e desenvolver no novo módulo** |

Por exemplo, **forma quadrática** já está definida no Capítulo 2. Portanto, quando aparecer na densidade normal multivariada, não deve ser redefinida. Pode-se escrever, por exemplo:

> utilizando a forma quadrática definida em @def-forma-quadratica...

Por outro lado, a matriz de covariâncias aparece antecipadamente na relação entre SVD e covariância no Capítulo 6, mas ainda não foi formalmente construída como objeto probabilístico. Portanto, deverá ser definida de forma completa no Capítulo 8.

A organização dos módulos deve preservar a progressão:

$$
\text{representação e geometria}
\longrightarrow
\text{decomposições e cálculo}
\longrightarrow
\text{probabilidade e inferência}
\longrightarrow
\text{técnicas multivariadas}.
$$

---

# Capítulo 1 — Vetores e representação dos dados multivariados

## Função do capítulo

O Capítulo 1 estabelece a linguagem inicial utilizada em todo o livro. A observação multivariada passa a ser representada como um vetor em $\mathbb R^p$, enquanto um conjunto com $n$ observações e $p$ variáveis é organizado em uma matriz de dados.

O capítulo também estabelece a geometria euclidiana necessária aos capítulos posteriores, incluindo produto interno, norma, ângulo, ortogonalidade e distância.

Esses conceitos sustentam posteriormente:

- matrizes de covariâncias e correlações;
- projeções;
- transformações;
- SVD;
- ACP;
- análise discriminante;
- análise de agrupamentos;
- MDS;
- outras técnicas baseadas em distâncias, direções ou combinações lineares.

## Convenções gerais de notação

O capítulo estabelece convenções que devem permanecer nos módulos seguintes:

- escalar observado: $a$, $x$, $\alpha$;
- vetor observado: $\mathbf x$, $\mathbf y$;
- matriz: $\mathbf A$, $\mathbf X$;
- variável aleatória escalar: $X$;
- vetor aleatório: $\boldsymbol{\mathsf X}$;
- realização de um vetor aleatório: $\mathbf x$;
- $a_{ij}$: elemento de uma matriz;
- $\mathbf a_{i\cdot}^{\top}$: linha $i$;
- $\mathbf a_{\cdot j}$: coluna $j$;
- $\mathbf x_i\in\mathbb R^p$: observação $i$ como vetor-coluna;
- $\mathbf x_i^\top$: a mesma observação armazenada como linha da matriz de dados;
- $\mathbf x_{\cdot j}\in\mathbb R^n$: variável $j$;
- $\mathbf0$: vetor ou matriz nula, com dimensão determinada pelo contexto.

### Uso posterior

Essa convenção deve ser herdada integralmente pelos módulos seguintes. Não é necessário redefinir a orientação de observações, variáveis, linhas e colunas.

---

## Escalares, vetores e matrizes

### Escalar

Define um escalar como um elemento de $\mathbb R$.

**Identificador:** `@def-escalar`.

### Vetor aleatório e vetor observado

Distingue explicitamente:

$$
\boldsymbol{\mathsf X}
$$

como vetor aleatório e

$$
\mathbf x
$$

como realização observada.

**Identificador:** `@def-vetor-multivariado`.

**Exemplo:** `@exm-vetor-observado`.

### Matriz

Define uma matriz, suas dimensões e seus elementos.

**Identificador:** `@def-matriz`.

### Vetor-coluna, vetor-linha e transposição

Estabelece a diferença entre

$$
\mathbf x
$$

e

$$
\mathbf x^\top.
$$

As observações são tratadas conceitualmente como vetores-coluna, mas armazenadas como linhas da matriz de dados.

**Identificador:** `@def-transposicao-vetor`.

**Tabela:** `@tbl-notacao-capitulo1`.

### Uso posterior

No Módulo II, não é necessário redefinir um vetor multivariado desde o início. A definição já existente pode ser retomada para construir média, covariância e distribuições.

---

## O espaço $\mathbb R^p$

Define o espaço dos vetores reais de dimensão $p$ e interpreta cada observação como um ponto.

É desenvolvida a progressão

$$
\mathbb R
\longrightarrow
\mathbb R^2
\longrightarrow
\mathbb R^3
\longrightarrow
\mathbb R^p.
$$

Também é discutido que, para $p>3$, a representação gráfica direta deixa de ser possível, mas as estruturas geométricas continuam válidas algebricamente.

**Definição:** `@def-espaco-rp`.

**Exemplo:** `@exm-observacao-r2`.

**Figuras:**

- `@fig-dimensoes-rp`;
- `@fig-vetor-observacao-r2`.

### Uso posterior

Não é necessário voltar a explicar o significado de uma observação em $\mathbb R^p$ quando forem apresentados:

- vetor normal multivariado;
- distância de Mahalanobis;
- ACP;
- análise discriminante;
- agrupamentos;
- MDS.

---

## Representação matricial de um conjunto de dados

Define explicitamente

$$
\mathbf X
\in
\mathbb R^{n\times p},
$$

em que:

- $n$ representa o número de observações;
- $p$ representa o número de variáveis;
- as observações são armazenadas nas linhas;
- as variáveis são armazenadas nas colunas.

Também são estabelecidas as notações:

- $x_{ij}$;
- $\mathbf x_i$;
- $\mathbf x_i^\top$;
- $\mathbf x_{\cdot j}$.

**Definição:** `@def-matriz-dados`.

**Exemplo:** `@exm-matriz-dados`.

### Espaço das observações e espaço das variáveis

É introduzida a dualidade:

- observações como vetores em $\mathbb R^p$;
- variáveis como vetores em $\mathbb R^n$.

**Tabela:** `@tbl-espacos-observacoes-variaveis`.

### Uso posterior

Essa distinção será central em:

- produtos cruzados;
- matrizes de covariâncias;
- SVD;
- ACP;
- análise de correspondência;
- problemas que distinguem o espaço das observações e o espaço das variáveis.

---

## Operações elementares com vetores

### Soma e subtração

São desenvolvidas a soma e a diferença entre vetores, juntamente com sua interpretação geométrica.

**Exemplo:** `@exm-soma-subtracao`.

**Definição do vetor nulo:** `@def-vetor-nulo`.

**Figura:** `@fig-soma-vetores`.

### Multiplicação por escalar

Mostra:

- expansão;
- contração;
- mudança de sentido.

**Exemplo:** `@exm-multiplicacao-escalar`.

### Combinações lineares

Define

$$
a_1\mathbf v_1
+
\cdots
+
a_k\mathbf v_k.
$$

Também mostra a construção de uma nova variável como combinação linear das variáveis originais.

**Definição:** `@def-combinacao-linear`.

**Exemplos:**

- `@exm-combinacao-vetores`;
- `@exm-combinacao-variaveis`.

### Uso posterior

Não redefinir combinação linear em:

- variância de $\mathbf a^\top\boldsymbol{\mathsf X}$;
- ACP;
- análise discriminante;
- correlação canônica;
- MANOVA;
- transformações lineares.

---

## Produto interno, norma, ângulo e distância euclidiana

### Produto interno euclidiano

Define

$$
\langle
\mathbf x,
\mathbf y
\rangle
=
\mathbf x^\top\mathbf y.
$$

São desenvolvidas suas propriedades algébricas e sua interpretação como medida de alinhamento.

**Definição:** `@def-produto-interno-euclidiano`.

**Propriedades:** `@prp-propriedades-produto-interno`.

**Exemplo:** `@exm-produto-interno`.

### Norma euclidiana

Define

$$
\|\mathbf x\|
=
\sqrt{
\mathbf x^\top\mathbf x
}.
$$

**Definição:** `@def-norma-euclidiana`.

**Exemplo:** `@exm-norma-euclidiana`.

**Figura:** `@fig-norma-vetor`.

### Observação sobre $\ell_1$ e $\ell_2$

A norma formalmente definida no Módulo I é a **norma euclidiana**.

Ela corresponde matematicamente à norma $\ell_2$, mas essa nomenclatura não foi formalmente introduzida.

A norma $\ell_1$ não foi definida.

Portanto:

- se $\ell_2$ for usada apenas como sinônimo da norma euclidiana, pode-se remeter a `@def-norma-euclidiana`;
- se $\ell_1$ for necessária posteriormente, deverá ser definida quando surgir.

---

## Desigualdade de Cauchy–Schwarz

O capítulo estabelece

$$
|
\mathbf x^\top\mathbf y
|
\leq
\|\mathbf x\|
\|\mathbf y\|.
$$

**Teorema:** `@thm-cauchy-schwarz`.

### Uso posterior

Pode ser diretamente referenciado para:

- limites de covariâncias;
- limites de correlações;
- maximização de produtos internos;
- geometria de combinações lineares.

---

## Ângulo entre vetores

Define o ângulo a partir do produto interno normalizado.

**Definição:** `@def-angulo-vetores`.

### Ortogonalidade

Define

$$
\mathbf x^\top\mathbf y
=
0.
$$

**Definição:** `@def-ortogonalidade`.

**Figura:** `@fig-produto-interno`.

---

## Distância euclidiana

Define

$$
d_E(\mathbf x,\mathbf y)
=
\|\mathbf x-\mathbf y\|.
$$

**Definição:** `@def-distancia-euclidiana`.

**Exemplo:** `@exm-distancia-euclidiana`.

**Figura:** `@fig-distancia-euclidiana`.

O capítulo também chama atenção para a dependência da distância em relação à escala das variáveis.

### Uso posterior

Essa observação será importante para:

- padronização;
- ACP;
- agrupamentos;
- MDS.

---

## Desigualdade triangular

Formaliza uma propriedade fundamental da distância euclidiana.

**Teorema:** `@thm-desigualdade-triangular`.

---

## Produto externo

Define

$$
\mathbf x\mathbf y^\top.
$$

**Definição:** `@def-produto-externo`.

**Exemplo:** `@exm-produto-externo`.

### Uso posterior

O produto externo reaparece em:

- matriz de covariâncias;
- matrizes SSCP;
- decomposição espectral;
- SVD;
- matrizes de posto 1.

---

# Capítulo 2 — Álgebra matricial aplicada

## Função do capítulo

O Capítulo 2 reúne o repertório matricial utilizado em todo o restante do livro:

- operações matriciais;
- transposição;
- simetria;
- traço;
- identidade;
- inversa;
- determinante;
- posto;
- sistemas lineares;
- classes especiais de matrizes;
- formas bilineares;
- formas quadráticas;
- positividade;
- matrizes particionadas;
- complemento de Schur.

As formas quadráticas e o complemento de Schur têm importância especial para a Estatística Multivariada.

## Convenções matriciais

São estabelecidas, entre outras:

- $\mathbf A^{-1}$;
- $\mathbf A^+$;
- $\mathbf I_k$;
- $\det(\mathbf A)$;
- $\operatorname{tr}(\mathbf A)$;
- $\operatorname{posto}(\mathbf A)$.

Essas notações devem permanecer nos módulos seguintes.

---

## Operações matriciais fundamentais

### Igualdade

**Definição:** `@def-igualdade-matrizes`.

### Soma e subtração

**Definição:** `@def-soma-subtracao-matrizes`.

**Propriedades:** `@prp-propriedades-adicao-matricial`.

**Exemplo:** `@exm-soma-subtracao-matrizes`.

### Multiplicação por escalar

- `@def-multiplicacao-matriz-escalar`;
- `@prp-propriedades-multiplicacao-escalar`;
- `@exm-multiplicacao-matriz-escalar`.

### Produto matriz-vetor

**Definição:** `@def-produto-matriz-vetor`.

**Exemplo:** `@exm-produto-matriz-vetor`.

A operação é interpretada como combinação linear das colunas da matriz.

### Produto entre matrizes

Define

$$
\mathbf A\mathbf B
$$

e enfatiza a compatibilidade de dimensões.

**Definição:** `@def-produto-matricial`.

**Exemplo:** `@exm-produto-matricial`.

### Produto interno e produto externo como produtos matriciais

Mostra que os produtos vetoriais do Capítulo 1 são casos particulares do produto matricial.

**Exemplo:** `@exm-produtos-matriz-dados`.

### Propriedades do produto matricial

- `@prp-propriedades-produto-matricial`;
- `@exm-nao-comutatividade`.

### Uso posterior

Não é necessário reapresentar as propriedades elementares do produto matricial em modelos como

$$
\mathbf Y
=
\mathbf X\mathbf B+\mathbf E.
$$

---

## Transposição, simetria e traço

### Transposição

- `@def-transposta-matriz`;
- `@prp-propriedades-transposicao`;
- `@exm-transposta-matriz`;
- `@exm-transposta-produto`.

### Matrizes simétricas

- `@def-matriz-simetrica`;
- `@def-matriz-antissimetrica`;
- `@thm-decomposicao-simetrica-antissimetrica`;
- `@exm-matriz-simetrica`;
- `@exm-decomposicao-simetrica`.

### Uso posterior

Não redefinir simetria quando forem introduzidas:

- matrizes de covariâncias;
- matrizes de correlações;
- matrizes de Gram;
- Hessianas;
- matrizes de hipótese e erro;
- matrizes de dispersão.

---

## Traço

Define

$$
\operatorname{tr}(\mathbf A).
$$

**Definição:** `@def-traco`.

**Propriedades:**

- `@prp-propriedades-basicas-traco`;
- `@prp-ciclicidade-multiplos-fatores`;
- `@prp-traco-produto-externo`;
- `@prp-traco-produtos-matriciais`.

**Exemplos:**

- `@exm-traco`;
- `@exm-propriedade-ciclica-traco`.

### Uso posterior

Referenciar diretamente em:

- variância total;
- critérios de máxima verossimilhança;
- cálculo matricial;
- funções envolvendo matrizes de covariâncias;
- MANOVA.

---

## Identidade, inversa, determinante, posto e sistemas lineares

### Matriz identidade

- `@def-matriz-identidade`;
- `@exm-matriz-identidade`.

### Matriz inversa

- `@def-matriz-inversa`;
- `@thm-unicidade-inversa`;
- `@thm-inversa-produto`;
- `@exm-matriz-inversa`.

### Determinante

São definidos menores, cofatores e determinante.

- `@def-menor-cofator`;
- `@def-determinante`;
- `@prp-propriedades-determinante`;
- `@thm-determinante-invertibilidade`.

**Exemplos:**

- `@exm-determinante-ordem-dois`;
- `@exm-determinante-ordem-tres`;
- `@exm-determinante-invertibilidade`.

A interpretação geométrica do determinante como fator de alteração de área ou volume também é desenvolvida.

**Figura:** `@fig-interpretacao-determinante`.

### Uso posterior

Não redefinir determinante em:

- densidade normal multivariada;
- variância generalizada;
- transformações de densidades;
- critérios de Wilks;
- Jacobianos.

---

## Posto

**Definição:** `@def-posto-matriz`.

**Teorema:** `@thm-equivalencias-invertibilidade`.

**Exemplo:** `@exm-posto-matrizes`.

### Uso posterior

Será central para:

- singularidade da matriz de covariâncias;
- identificabilidade;
- modelos lineares;
- SVD;
- problemas de alta dimensão;
- dimensionalidade máxima das técnicas.

---

## Sistemas lineares

**Definição:** `@def-sistema-linear`.

**Teorema:** `@thm-existencia-solucoes-sistema`.

**Tabela:** `@tbl-classificacao-sistemas`.

**Exemplos:**

- `@exm-sistema-solucao-unica`;
- `@exm-sistemas-infinitas-sem-solucao`.

### Aprofundamento: pseudoinversa

A pseudoinversa é apresentada inicialmente ainda no Capítulo 2.

**Definição:** `@def-pseudoinversa-moore-penrose`.

**Tabela:** `@tbl-casos-pseudoinversa`.

O desenvolvimento completo ocorre posteriormente no Capítulo 6.

---

## Matrizes ortogonais, diagonais e triangulares

### Matrizes ortogonais

- `@def-matriz-ortogonal`;
- `@thm-preservacao-produto-interno`;
- `@exm-matriz-ortogonal`.

### Matrizes diagonais

- `@def-matriz-diagonal`;
- `@exm-produto-matriz-diagonal`.

### Matrizes triangulares

- `@def-matriz-triangular`;
- `@exm-matriz-triangular`.

### Aprofundamento: sistemas triangulares

**Exemplo:** `@exm-substituicao-regressiva`.

---

## Formas bilineares, formas quadráticas e positividade

### Forma bilinear

Define

$$
\mathbf x^\top
\mathbf A
\mathbf y.
$$

**Definição:** `@def-forma-bilinear`.

**Exemplo:** `@exm-forma-bilinear`.

### Forma quadrática

Define

$$
\mathbf x^\top
\mathbf A
\mathbf x.
$$

**Definição:** `@def-forma-quadratica`.

**Teorema:** `@thm-parte-simetrica-forma-quadratica`.

**Exemplo:** `@exm-forma-quadratica`.

### Uso posterior

A definição deve ser referenciada, e não refeita, em:

- variância de combinações lineares;
- distância de Mahalanobis;
- normal multivariada;
- análise discriminante;
- MANOVA;
- ACP;
- quocientes de Rayleigh.

---

## Positividade

São formalizadas as classificações de matrizes simétricas:

- positiva definida;
- positiva semidefinida;
- negativa definida;
- negativa semidefinida;
- indefinida.

**Definição:** `@def-classificacao-positividade`.

**Exemplo:** `@exm-positividade-matrizes`.

**Figura:** `@fig-formas-quadraticas`.

### Uso posterior

Essa definição será central para:

- matrizes de covariâncias;
- matrizes de correlações;
- distância de Mahalanobis;
- normal multivariada;
- Hessianas;
- problemas de otimização.

---

## Aprofundamento: matrizes particionadas e complemento de Schur

### Matrizes particionadas

**Definição:** `@def-matriz-particionada`.

**Exemplo:** `@exm-matriz-particionada`.

São trabalhadas:

- soma por blocos;
- transposição por blocos;
- multiplicação por blocos.

**Exemplo:** `@exm-produto-blocos`.

### Complemento de Schur

**Definição:** `@def-complemento-schur`.

**Exemplo:** `@exm-complemento-schur`.

### Determinante em blocos

**Teorema:** `@thm-determinante-complemento-schur`.

### Inversão em blocos

**Teorema:** `@thm-inversa-complemento-schur`.

### Positividade e complemento de Schur

**Teorema:** `@thm-positividade-complemento-schur`.

### Uso posterior

Esse bloco deve ser fortemente reutilizado em:

- normal multivariada particionada;
- covariância condicional;
- regressão entre blocos;
- correlação canônica;
- critérios de positividade.

**Tabela de síntese:** `@tbl-sintese-algebra-matricial`.

---

# Capítulo 3 — Espaços vetoriais, bases e projeções

## Função do capítulo

O Capítulo 3 organiza a geometria linear utilizada posteriormente em:

- posto;
- subespaços;
- independência linear;
- espaços fundamentais;
- geometrias induzidas;
- matrizes de Gram;
- projeções;
- mínimos quadrados.

Na versão consolidada atual, não há uma seção geral independente dedicada a dissimilaridades e similaridades. Quando esses conceitos forem necessários em agrupamentos ou MDS, poderão ser formalizados nos capítulos correspondentes.

## Convenções para espaços vetoriais e subespaços

São utilizadas, entre outras:

- $V$, $W$, $S$;
- $\mathcal B$;
- $\dim(V)$;
- $\operatorname{span}$;
- $[\mathbf x]_{\mathcal B}$;
- $S^\perp$;
- $\mathcal C(\mathbf A)$;
- $\mathcal L(\mathbf A)$;
- $\mathcal N(\mathbf A)$;
- $\mathcal N(\mathbf A^\top)$;
- $\oplus$;
- $\operatorname{proj}_S(\mathbf x)$;
- $\mathbf P_S$.

Essas notações devem ser mantidas nos módulos seguintes.

---

## Espaços, subespaços e geração

### Espaço vetorial

**Definição:** `@def-espaco-vetorial`.

### Subespaços vetoriais

- `@def-subespaco-vetorial`;
- `@prp-criterio-subespaco`;
- `@exm-reta-subespaco`;
- `@exm-reta-nao-subespaco`.

**Figura:** `@fig-subespaco`.

### Subespaço gerado

- `@def-subespaco-gerado`;
- `@exm-subespaco-gerado-reta`;
- `@exm-subespaco-gerado-plano`.

---

## Independência linear, bases e dimensão

### Independência linear

**Definição:** `@def-independencia-linear`.

**Exemplos:**

- `@exm-vetores-dependentes`;
- `@exm-vetores-independentes`.

**Figura:** `@fig-independencia-linear`.

### Independência linear e sistemas homogêneos

- `@prp-independencia-sistema-homogeneo`;
- `@prp-propriedades-independencia`;
- `@prp-dependencia-redundancia`.

### Uso posterior

Redundância entre variáveis deve remeter a esse conteúdo em situações envolvendo:

- singularidade;
- multicolinearidade;
- redução de dimensão;
- posto de matrizes de covariâncias.

---

## Bases

- `@def-base`;
- `@exm-base-r2`;
- `@exm-bases-diferentes`.

### Coordenadas em uma base

- `@thm-representacao-unica-base`;
- `@def-coordenadas-base`;
- `@exm-coordenadas-base`.

### Base canônica

`@def-base-canonica`.

### Bases ortogonais e ortonormais

- `@def-base-ortogonal`;
- `@def-base-ortonormal`;
- `@thm-existencia-base-ortonormal`;
- `@prp-coordenadas-base-ortogonal`.

**Figura:** `@fig-bases-diferentes`.

### Dimensão

- `@thm-cardinalidade-bases`;
- `@def-dimensao`;
- `@prp-independencia-geracao-dimensao`;
- `@exm-dimensao-subespacos`.

---

## Espaços fundamentais de uma matriz

### Espaço nulo

- `@def-espaco-nulo`;
- `@prp-espaco-nulo-subespaco`;
- `@exm-espaco-nulo`.

### Espaço coluna

- `@def-espaco-coluna`;
- `@prp-consistencia-espaco-coluna`;
- `@exm-espaco-coluna`.

### Espaço linha

- `@def-espaco-linha`;
- `@exm-espaco-linha`.

### Espaço nulo à esquerda

- `@def-espaco-nulo-esquerda`;
- `@exm-espaco-nulo-esquerda`.

### Extensão de uma base

`@thm-extensao-base`.

### Teorema Posto–Nulidade

- `@thm-posto-nulidade`;
- `@exm-posto-nulidade`.

### Os quatro espaços fundamentais

`@tbl-espacos-fundamentais`.

### Complemento ortogonal

`@def-complemento-ortogonal`.

### Relações de ortogonalidade

`@thm-ortogonalidade-espacos-fundamentais`.

### Uso posterior

Esses resultados devem ser reutilizados em:

- resíduos de regressão;
- SVD;
- equações normais;
- projeções;
- identificabilidade;
- modelo linear multivariado.

---

## Produtos internos, bases ortonormais e matrizes de Gram

### Aprofundamento: produtos internos e geometrias induzidas

Define

$$
\langle
\mathbf x,
\mathbf y
\rangle_{\mathbf M}
=
\mathbf x^\top
\mathbf M
\mathbf y,
$$

para

$$
\mathbf M
\succ
\mathbf0.
$$

**Definição:** `@def-produto-interno-induzido`.

**Propriedades:** `@prp-propriedades-produto-interno-induzido`.

**Exemplo:** `@exm-produto-interno-induzido`.

### Norma e distância induzidas

Define

$$
\|\mathbf x\|_{\mathbf M}
=
\sqrt{
\mathbf x^\top
\mathbf M
\mathbf x
}.
$$

A distância correspondente é obtida pela norma da diferença.

**Definição:** `@def-norma-induzida`.

**Exemplo:** `@exm-norma-distancia-induzidas`.

### Ortogonalidade induzida e relação com a geometria euclidiana

O capítulo mostra que uma geometria ponderada pode ser transformada em uma geometria euclidiana mediante transformação apropriada.

**Figura:** `@fig-geometrias-induzidas`.

### Uso posterior

Essa estrutura é a base matemática para:

- distância de Mahalanobis;
- mínimos quadrados ponderados;
- autovalores generalizados;
- análise discriminante;
- correlação canônica.

---

## Matrizes de Gram

Define a matriz formada por produtos internos.

**Definição:** `@def-matriz-gram`.

**Exemplo:** `@exm-matriz-gram`.

**Propriedades:** `@prp-propriedades-matriz-gram`.

### Matrizes de Gram e bases ortonormais

`@exm-gram-base-ortogonal`.

### Produtos cruzados em uma matriz de dados

**Tabela:** `@tbl-produtos-gram`.

São trabalhadas diretamente as matrizes

$$
\mathbf X^\top\mathbf X
$$

e

$$
\mathbf X\mathbf X^\top.
$$

### Uso posterior

Referenciar em:

- covariâncias amostrais;
- SVD;
- MDS;
- ACP;
- análise de correspondência.

---

## Complemento ortogonal

### Propriedades

- `@prp-complemento-ortogonal-subespaco`;
- `@exm-complemento-ortogonal`.

### Dimensão

`@prp-dimensao-complemento-ortogonal`.

### Decomposição ortogonal

`@thm-decomposicao-ortogonal`.

### Decomposição dos espaços fundamentais

`@prp-decomposicao-espacos-fundamentais`.

---

## Projeções ortogonais

### Definição geral

`@def-projecao-ortogonal`.

### Projeção sobre uma direção

- `@prp-projecao-direcao`;
- `@exm-projecao-direcao`.

**Figura:** `@fig-projecao-ortogonal`.

### Projeção sobre subespaço com base ortonormal

- `@prp-projecao-base-ortonormal`;
- `@exm-projecao-base-ortonormal`.

### Projeção sobre o espaço coluna

- `@prp-projecao-espaco-coluna`;
- `@exm-projecao-espaco-coluna`.

### Propriedades da matriz de projeção

`@prp-propriedades-matriz-projecao`.

### Aprofundamento: projeção residual

**Definição:** `@def-matriz-projecao-residual`.

**Propriedades:** `@prp-propriedades-projecao-residual`.

### Projeção como melhor aproximação

`@thm-melhor-aproximacao`.

### Aprofundamento: projeções e mínimos quadrados

**Definição das equações normais:** `@def-equacoes-normais`.

**Proposição:** `@prp-decomposicao-minimos-quadrados`.

### Uso posterior

Esse conjunto é fundamental para:

- regressão multivariada;
- MANOVA;
- ACP como projeção;
- mínimos quadrados;
- resíduos;
- matrizes de hipótese e erro.

# Índice operacional do Módulo I

Este índice complementa o inventário descritivo do Módulo I. Seu objetivo é permitir uma consulta rápida durante a elaboração dos Módulos II e III.

A lógica de consulta é:

1. identificar qual estrutura matemática aparece no novo capítulo;
2. verificar se ela já possui definição, proposição ou teorema no Módulo I;
3. utilizar a referência cruzada correspondente;
4. desenvolver no novo capítulo apenas a interpretação probabilística, inferencial ou metodológica que ainda não foi apresentada.

---

# Capítulo 1 — Índice de objetos vetoriais e geometria euclidiana

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Escalar | `@def-escalar` |
| Vetor multivariado | `@def-vetor-multivariado` |
| Matriz | `@def-matriz` |
| Transposição de vetor | `@def-transposicao-vetor` |
| Espaço $\mathbb R^p$ | `@def-espaco-rp` |
| Matriz de dados | `@def-matriz-dados` |
| Vetor nulo | `@def-vetor-nulo` |
| Combinação linear | `@def-combinacao-linear` |
| Produto interno euclidiano | `@def-produto-interno-euclidiano` |
| Norma euclidiana | `@def-norma-euclidiana` |
| Ângulo entre vetores | `@def-angulo-vetores` |
| Ortogonalidade | `@def-ortogonalidade` |
| Distância euclidiana | `@def-distancia-euclidiana` |
| Produto externo | `@def-produto-externo` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Propriedades do produto interno | `@prp-propriedades-produto-interno` |
| Desigualdade de Cauchy--Schwarz | `@thm-cauchy-schwarz` |
| Desigualdade triangular | `@thm-desigualdade-triangular` |

## Exemplos de referência

| Conteúdo | Identificador |
|:---|:---|
| Vetor observado | `@exm-vetor-observado` |
| Observação em $\mathbb R^2$ | `@exm-observacao-r2` |
| Matriz de dados | `@exm-matriz-dados` |
| Soma e subtração | `@exm-soma-subtracao` |
| Multiplicação por escalar | `@exm-multiplicacao-escalar` |
| Combinação de vetores | `@exm-combinacao-vetores` |
| Combinação de variáveis | `@exm-combinacao-variaveis` |
| Produto interno | `@exm-produto-interno` |
| Norma euclidiana | `@exm-norma-euclidiana` |
| Distância euclidiana | `@exm-distancia-euclidiana` |
| Produto externo | `@exm-produto-externo` |

## Figuras e tabelas

| Conteúdo | Identificador |
|:---|:---|
| Notação do capítulo | `@tbl-notacao-capitulo1` |
| Espaços das observações e variáveis | `@tbl-espacos-observacoes-variaveis` |
| Dimensões de $\mathbb R^p$ | `@fig-dimensoes-rp` |
| Vetor-observação | `@fig-vetor-observacao-r2` |
| Soma de vetores | `@fig-soma-vetores` |
| Norma | `@fig-norma-vetor` |
| Produto interno e geometria | `@fig-produto-interno` |
| Distância euclidiana | `@fig-distancia-euclidiana` |

## Principais usos posteriores

### Módulo II

- definição de vetor aleatório;
- esperança vetorial;
- covariância;
- combinações lineares;
- transformações de vetores aleatórios;
- geometria da normal multivariada.

### Módulo III

- ACP;
- análise discriminante;
- correlação canônica;
- agrupamentos;
- MDS.

---

# Capítulo 2 — Índice de álgebra matricial

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Igualdade de matrizes | `@def-igualdade-matrizes` |
| Soma e subtração | `@def-soma-subtracao-matrizes` |
| Multiplicação por escalar | `@def-multiplicacao-matriz-escalar` |
| Produto matriz-vetor | `@def-produto-matriz-vetor` |
| Produto matricial | `@def-produto-matricial` |
| Transposta | `@def-transposta-matriz` |
| Matriz simétrica | `@def-matriz-simetrica` |
| Matriz antissimétrica | `@def-matriz-antissimetrica` |
| Traço | `@def-traco` |
| Matriz identidade | `@def-matriz-identidade` |
| Matriz inversa | `@def-matriz-inversa` |
| Menor e cofator | `@def-menor-cofator` |
| Determinante | `@def-determinante` |
| Posto | `@def-posto-matriz` |
| Sistema linear | `@def-sistema-linear` |
| Pseudoinversa de Moore--Penrose | `@def-pseudoinversa-moore-penrose` |
| Matriz ortogonal | `@def-matriz-ortogonal` |
| Matriz diagonal | `@def-matriz-diagonal` |
| Matriz triangular | `@def-matriz-triangular` |
| Forma bilinear | `@def-forma-bilinear` |
| Forma quadrática | `@def-forma-quadratica` |
| Classificação por positividade | `@def-classificacao-positividade` |
| Matriz particionada | `@def-matriz-particionada` |
| Complemento de Schur | `@def-complemento-schur` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Propriedades da adição matricial | `@prp-propriedades-adicao-matricial` |
| Propriedades da multiplicação por escalar | `@prp-propriedades-multiplicacao-escalar` |
| Propriedades do produto matricial | `@prp-propriedades-produto-matricial` |
| Propriedades da transposição | `@prp-propriedades-transposicao` |
| Decomposição simétrica e antissimétrica | `@thm-decomposicao-simetrica-antissimetrica` |
| Propriedades básicas do traço | `@prp-propriedades-basicas-traco` |
| Ciclicidade com múltiplos fatores | `@prp-ciclicidade-multiplos-fatores` |
| Traço de produto externo | `@prp-traco-produto-externo` |
| Traço de produtos matriciais | `@prp-traco-produtos-matriciais` |
| Unicidade da inversa | `@thm-unicidade-inversa` |
| Inversa de produto | `@thm-inversa-produto` |
| Propriedades do determinante | `@prp-propriedades-determinante` |
| Determinante e invertibilidade | `@thm-determinante-invertibilidade` |
| Equivalências de invertibilidade | `@thm-equivalencias-invertibilidade` |
| Existência de soluções de sistemas | `@thm-existencia-solucoes-sistema` |
| Preservação do produto interno | `@thm-preservacao-produto-interno` |
| Parte simétrica de uma forma quadrática | `@thm-parte-simetrica-forma-quadratica` |
| Determinante pelo complemento de Schur | `@thm-determinante-complemento-schur` |
| Inversa pelo complemento de Schur | `@thm-inversa-complemento-schur` |
| Positividade e complemento de Schur | `@thm-positividade-complemento-schur` |

## Resultados particularmente importantes para o Módulo II

### Matriz simétrica

Usar:

`@def-matriz-simetrica`

quando forem introduzidas matrizes de covariâncias e correlações.

### Forma quadrática

Usar:

`@def-forma-quadratica`

para expressões como

$$
\mathbf a^\top
\boldsymbol{\Sigma}
\mathbf a
$$

e

$$
(
\mathbf x-\boldsymbol{\mu}
)^\top
\boldsymbol{\Sigma}^{-1}
(
\mathbf x-\boldsymbol{\mu}
).
$$

### Positividade

Usar:

`@def-classificacao-positividade`

para estabelecer que matrizes de covariâncias são semidefinidas positivas.

### Complemento de Schur

Usar:

`@def-complemento-schur`

e, conforme necessário,

- `@thm-determinante-complemento-schur`;
- `@thm-inversa-complemento-schur`;
- `@thm-positividade-complemento-schur`.

Aplicações futuras:

- covariância condicional;
- normal multivariada particionada;
- regressão entre blocos;
- correlação canônica.

---

# Capítulo 3 — Índice de espaços vetoriais e projeções

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Espaço vetorial | `@def-espaco-vetorial` |
| Subespaço | `@def-subespaco-vetorial` |
| Subespaço gerado | `@def-subespaco-gerado` |
| Independência linear | `@def-independencia-linear` |
| Base | `@def-base` |
| Coordenadas em uma base | `@def-coordenadas-base` |
| Base canônica | `@def-base-canonica` |
| Base ortogonal | `@def-base-ortogonal` |
| Base ortonormal | `@def-base-ortonormal` |
| Dimensão | `@def-dimensao` |
| Espaço nulo | `@def-espaco-nulo` |
| Espaço coluna | `@def-espaco-coluna` |
| Espaço linha | `@def-espaco-linha` |
| Espaço nulo à esquerda | `@def-espaco-nulo-esquerda` |
| Complemento ortogonal | `@def-complemento-ortogonal` |
| Produto interno induzido | `@def-produto-interno-induzido` |
| Norma induzida | `@def-norma-induzida` |
| Matriz de Gram | `@def-matriz-gram` |
| Projeção ortogonal | `@def-projecao-ortogonal` |
| Matriz de projeção residual | `@def-matriz-projecao-residual` |
| Equações normais | `@def-equacoes-normais` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Critério para subespaço | `@prp-criterio-subespaco` |
| Independência e sistema homogêneo | `@prp-independencia-sistema-homogeneo` |
| Propriedades da independência | `@prp-propriedades-independencia` |
| Dependência e redundância | `@prp-dependencia-redundancia` |
| Representação única em uma base | `@thm-representacao-unica-base` |
| Existência de base ortonormal | `@thm-existencia-base-ortonormal` |
| Coordenadas em base ortogonal | `@prp-coordenadas-base-ortogonal` |
| Cardinalidade das bases | `@thm-cardinalidade-bases` |
| Independência, geração e dimensão | `@prp-independencia-geracao-dimensao` |
| Espaço nulo como subespaço | `@prp-espaco-nulo-subespaco` |
| Consistência e espaço coluna | `@prp-consistencia-espaco-coluna` |
| Extensão de base | `@thm-extensao-base` |
| Posto--Nulidade | `@thm-posto-nulidade` |
| Ortogonalidade dos espaços fundamentais | `@thm-ortogonalidade-espacos-fundamentais` |
| Propriedades do produto interno induzido | `@prp-propriedades-produto-interno-induzido` |
| Propriedades da matriz de Gram | `@prp-propriedades-matriz-gram` |
| Propriedades do complemento ortogonal | `@prp-complemento-ortogonal-subespaco` |
| Dimensão do complemento ortogonal | `@prp-dimensao-complemento-ortogonal` |
| Decomposição ortogonal | `@thm-decomposicao-ortogonal` |
| Decomposição dos espaços fundamentais | `@prp-decomposicao-espacos-fundamentais` |
| Projeção sobre direção | `@prp-projecao-direcao` |
| Projeção em base ortonormal | `@prp-projecao-base-ortonormal` |
| Projeção sobre espaço coluna | `@prp-projecao-espaco-coluna` |
| Propriedades da matriz de projeção | `@prp-propriedades-matriz-projecao` |
| Propriedades do projetor residual | `@prp-propriedades-projecao-residual` |
| Melhor aproximação | `@thm-melhor-aproximacao` |
| Decomposição de mínimos quadrados | `@prp-decomposicao-minimos-quadrados` |

## Dependências futuras principais

### Distância de Mahalanobis

Usar:

- `@def-produto-interno-induzido`;
- `@def-norma-induzida`.

A novidade estatística será substituir $\mathbf M$ por uma matriz relacionada a $\boldsymbol{\Sigma}^{-1}$.

### Regressão

Usar:

- `@def-espaco-coluna`;
- `@thm-ortogonalidade-espacos-fundamentais`;
- `@prp-projecao-espaco-coluna`;
- `@def-matriz-projecao-residual`;
- `@thm-melhor-aproximacao`;
- `@def-equacoes-normais`.

### ACP

Usar:

- projeções;
- bases ortonormais;
- melhor aproximação.

---

# Capítulo 4 — Índice de transformações

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Transformação linear | `@def-transformacao-linear` |
| Núcleo e imagem | `@def-nucleo-imagem-transformacao` |
| Transformação inversa | `@def-transformacao-inversa` |
| Injetividade e sobrejetividade | `@def-injetividade-sobrejetividade` |
| Matriz de mudança de base | `@def-matriz-mudanca-base` |
| Matrizes semelhantes | `@def-matrizes-semelhantes` |
| Transformação afim | `@def-transformacao-afim` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Propriedades elementares | `@prp-propriedades-elementares-transformacao-linear` |
| Transformação determinada por base | `@thm-transformacao-determinada-base` |
| Representação matricial | `@thm-representacao-matricial-transformacao` |
| Transformação de matriz de dados | `@prp-transformacao-matriz-dados` |
| Matriz da composição | `@prp-matriz-composicao` |
| Inversa de composição | `@prp-inversa-composicao` |
| Caracterizações de transformação linear | `@thm-caracterizacoes-transformacao-linear` |
| Equivalências para invertibilidade | `@thm-equivalencias-transformacao-invertivel` |
| Determinante e volume | `@thm-determinante-volume-transformacao` |
| Preservação da geometria por transformação ortogonal | `@prp-preservacao-geometria-transformacao-ortogonal` |
| Gram sob transformação ortogonal | `@prp-gram-transformacao-ortogonal` |
| Determinante de matriz ortogonal | `@prp-determinante-matriz-ortogonal` |
| Transformação em bases gerais | `@thm-transformacao-bases-gerais` |
| Invariantes por semelhança | `@prp-invariantes-semelhanca` |
| Centralização preserva distâncias | `@prp-centralizacao-preserva-distancias` |

## Uso direto no Módulo II

### Transformação de vetor aleatório

A transformação

$$
\mathbf Y
=
\mathbf A
\boldsymbol{\mathsf X}
+
\mathbf b
$$

não precisa ter sua álgebra reconstruída.

Usar `@def-transformacao-afim`.

### Transformação da covariância

A novidade será mostrar probabilisticamente que

$$
\operatorname{Cov}
(
\mathbf A\boldsymbol{\mathsf X}
+
\mathbf b
)
=
\mathbf A
\boldsymbol{\Sigma}
\mathbf A^\top.
$$

### Normal multivariada

Transformações afins devem remeter a `@def-transformacao-afim`.

### Padronização e branqueamento

A centralização e o escalonamento já possuem interpretação geométrica no Capítulo 4.

O Módulo II deverá acrescentar a interpretação probabilística.

---

# Capítulo 5 — Índice espectral

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Autovalor e autovetor | `@def-autovalor-autovetor` |
| Autoespaço | `@def-autoespaco` |
| Polinômio característico | `@def-polinomio-caracteristico` |
| Multiplicidades | `@def-multiplicidades-autovalor` |
| Matriz diagonalizável | `@def-matriz-diagonalizavel` |
| Quociente de Rayleigh | `@def-quociente-rayleigh` |
| Função espectral | `@def-funcao-espectral` |
| Raiz quadrada principal | `@def-raiz-quadrada-principal` |
| Pseudoinversa espectral | `@def-pseudoinversa-espectral` |
| Autovalor generalizado | `@def-autovalor-generalizado` |
| Quociente de Rayleigh generalizado | `@def-quociente-rayleigh-generalizado` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Múltiplos de autovetor | `@prp-multiplos-autovetor` |
| Autoespaço invariante | `@prp-autoespaco-invariante` |
| Caracterização pelo determinante | `@thm-caracterizacao-autovalor-determinante` |
| Independência para autovalores distintos | `@thm-autovetores-autovalores-distintos` |
| Critérios de diagonalização | `@thm-criterios-diagonalizacao` |
| Teorema Espectral | `@thm-teorema-espectral` |
| Decomposição por projetores espectrais | `@prp-decomposicao-projetores-espectrais` |
| Consequências espectrais básicas | `@prp-consequencias-espectrais-basicas` |
| Núcleo e imagem pela decomposição espectral | `@prp-nucleo-imagem-espectral` |
| Positividade e autovalores | `@thm-positividade-autovalores` |
| Extremos do quociente de Rayleigh | `@thm-extremos-quociente-rayleigh` |
| Otimização restrita e autovalores | `@thm-autovalores-otimizacao-restrita` |
| Caracterização variacional sucessiva | `@thm-caracterizacao-variacional-sucessiva` |
| Função espectral e autoespaços | `@prp-funcao-espectral-autoespacos` |
| Extremos do Rayleigh generalizado | `@thm-extremos-rayleigh-generalizado` |
| Ortogonalidade dos autovetores generalizados | `@prp-ortogonalidade-autovetores-generalizados` |

## Uso direto no Módulo II

### Matriz de covariâncias

Uma vez demonstrado no Capítulo 8 que

$$
\boldsymbol{\Sigma}
=
\boldsymbol{\Sigma}^\top
$$

e

$$
\boldsymbol{\Sigma}
\succeq
\mathbf0,
$$

pode-se aplicar diretamente:

- `@thm-teorema-espectral`;
- `@thm-positividade-autovalores`.

### Variância direcional

Para

$$
\operatorname{Var}
(
\mathbf a^\top
\boldsymbol{\mathsf X}
)
=
\mathbf a^\top
\boldsymbol{\Sigma}
\mathbf a,
$$

a busca das direções extremas deve remeter a:

- `@def-quociente-rayleigh`;
- `@thm-extremos-quociente-rayleigh`.

### Raiz de matriz

Usar `@def-raiz-quadrada-principal` na construção de:

- branqueamento;
- transformações esféricas;
- normal multivariada;
- Mahalanobis.

---

# Capítulo 6 — Índice de SVD

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Matriz diagonal retangular | `@def-matriz-diagonal-retangular` |
| Valores e vetores singulares | `@def-vetores-valores-singulares` |
| SVD completa | `@def-svd-completa` |
| SVD compacta | `@def-svd-compacta` |
| SVD truncada | `@def-svd-truncada` |
| Produto interno de Frobenius | `@def-produto-interno-frobenius` |
| Norma de Frobenius | `@def-norma-frobenius` |
| Norma espectral | `@def-norma-espectral` |
| Número de condição | `@def-numero-condicao-svd` |
| Posto numérico | `@def-posto-numerico` |
| Pseudoinversa por SVD | `@def-pseudoinversa-svd` |
| Solução de mínimos quadrados | `@def-solucao-minimos-quadrados` |

## Proposições e teoremas

| Resultado | Identificador |
|:---|:---|
| Existência da SVD | `@thm-existencia-svd` |
| Espectros das matrizes de Gram | `@prp-espectros-gram-svd` |
| Construção dos pares singulares | `@prp-construcao-pares-singulares` |
| Construção da SVD compacta | `@prp-construcao-svd-compacta` |
| Decomposições espectrais associadas | `@prp-decomposicoes-espectrais-svd` |
| Imagem da bola | `@prp-imagem-bola-svd` |
| Extremos dos comprimentos | `@prp-extremos-comprimentos-svd` |
| Espaços fundamentais pela SVD | `@prp-espacos-fundamentais-svd` |
| Projetores pela SVD | `@prp-projetores-espacos-svd` |
| Normas e valores singulares | `@prp-normas-valores-singulares` |
| Invariância ortogonal das normas | `@prp-invariancia-ortogonal-normas` |
| Dualidade da SVD da matriz de dados | `@prp-dualidade-svd-dados` |
| Geometria das coordenadas singulares | `@prp-geometria-coordenadas-singulares` |
| Centralização e vetores singulares | `@prp-centralizacao-vetores-singulares` |
| SVD e covariância amostral | `@prp-svd-covariancia-amostral` |
| Magnitude quadrática | `@prp-magnitude-quadratica-svd-dados` |
| Forma expandida | `@prp-forma-expandida-svd` |
| Ortogonalidade das parcelas | `@prp-ortogonalidade-parcelas-svd` |
| Erros da SVD truncada | `@prp-erros-svd-truncada` |
| Eckart--Young--Mirsky | `@thm-eckart-young-mirsky` |
| Aproximação de dados por projeção | `@prp-aproximacao-dados-projecao-svd` |
| Condições de Moore--Penrose | `@thm-condicoes-moore-penrose` |
| Projetores da pseudoinversa | `@prp-projetores-pseudoinversa` |
| Sistemas compatíveis | `@prp-sistemas-compativeis-pseudoinversa` |
| Mínimos quadrados por pseudoinversa | `@prp-minimos-quadrados-pseudoinversa` |

## Uso futuro mais importante

### ACP

Usar:

- `@prp-svd-covariancia-amostral`;
- `@thm-eckart-young-mirsky`;
- `@prp-aproximacao-dados-projecao-svd`.

### Análise de Correspondência

Usar:

- SVD;
- norma de Frobenius;
- aproximações de baixo posto;
- dualidade entre linhas e colunas.

### Modelo linear

Usar:

- pseudoinversa;
- projetores;
- solução de mínimos quadrados.

---

# Capítulo 7 — Índice de cálculo vetorial, matricial e integral

## Definições

| Conteúdo | Identificador |
|:---|:---|
| Diferenciabilidade escalar | `@def-diferenciabilidade-escalar` |
| Diferenciabilidade vetorial | `@def-diferenciabilidade-vetorial` |
| Derivada direcional | `@def-derivada-direcional` |
| Jacobiana | `@def-jacobiana` |
| Hessiana | `@def-hessiana` |
| Ponto estacionário | `@def-ponto-estacionario` |
| Conjunto convexo | `@def-conjunto-convexo` |
| Função convexa | `@def-funcao-convexa` |
| Operador `vec` | `@def-operador-vec` |
| Produto de Kronecker | `@def-produto-kronecker` |
| Matriz de comutação | `@def-matriz-comutacao` |
| Operador `vech` | `@def-operador-vech` |
| Matriz de duplicação | `@def-matriz-duplicacao` |
| Matriz de eliminação | `@def-matriz-eliminacao` |
| Densidade conjunta | `@def-densidade-conjunta` |
| Vetor escore | `@def-vetor-escore` |
| Informação observada | `@def-informacao-observada` |
| Informação de Fisher | `@def-informacao-fisher` |

## Teoremas

| Resultado | Identificador |
|:---|:---|
| Regra da cadeia | `@thm-regra-cadeia` |
| Taylor de segunda ordem | `@thm-taylor-segunda-ordem` |
| Classificação pela Hessiana | `@thm-classificacao-hessiana` |
| Convexidade de primeira ordem | `@thm-convexidade-primeira-ordem` |
| Convexidade pela Hessiana | `@thm-convexidade-hessiana` |
| Multiplicadores de Lagrange | `@thm-multiplicadores-lagrange` |
| Tonelli | `@thm-tonelli` |
| Fubini | `@thm-fubini` |
| Mudança de variáveis | `@thm-mudanca-variaveis-integral` |
| Convergência Dominada | `@thm-convergencia-dominada` |
| Diferenciação sob o sinal de integração | `@thm-diferenciacao-integral` |
| Método delta | `@thm-metodo-delta` |

## Proposições particularmente importantes

| Resultado | Identificador |
|:---|:---|
| Direção do gradiente | `@prp-gradiente-crescimento` |
| Condição necessária de primeira ordem | `@prp-condicao-primeira-ordem-extremo-local` |
| Convexidade e minimização | `@prp-convexidade-minimizacao` |
| Direção de Newton | `@prp-direcao-newton-descida` |
| Derivadas de forma quadrática | `@prp-derivadas-forma-quadratica` |
| Equações normais pelo cálculo | `@prp-equacoes-normais-calculo` |
| Gradiente de resíduos não lineares | `@prp-gradiente-residuos-nao-lineares` |
| Gradiente de função linear matricial | `@prp-gradiente-funcao-linear-matriz` |
| Convexidade dos mínimos quadrados matriciais | `@prp-convexidade-minimos-quadrados-matriciais` |
| Diferencial da inversa | `@prp-diferencial-inversa` |
| Fórmula de Jacobi | `@prp-formula-jacobi` |
| Diferencial do log-determinante | `@prp-diferencial-logdet` |
| Concavidade do log-determinante | `@prp-concavidade-logdet` |
| Critério matricial de covariância | `@prp-minimizador-criterio-covariancia` |
| Identidade de vetorização | `@prp-identidade-vec` |
| Convexidade de forma quadrática matricial | `@prp-convexidade-forma-quadratica-matricial` |
| Gradiente para parâmetro simétrico | `@prp-gradiente-parametro-simetrico` |
| Densidades marginais | `@prp-densidades-marginais` |
| Transformação de densidade | `@prp-transformacao-densidade` |

---

# Matriz de dependências para o Capítulo 8

## Esperança e momentos

### Conhecimento prévio

- vetor multivariado: `@def-vetor-multivariado`;
- produto externo: `@def-produto-externo`;
- integração de funções vetoriais e matriciais do Capítulo 7.

### Conteúdo novo

- esperança de vetor aleatório;
- segundo momento;
- momento central;
- covariância.

---

## Matriz de covariâncias

### Conhecimento prévio

- matriz simétrica: `@def-matriz-simetrica`;
- forma quadrática: `@def-forma-quadratica`;
- positividade: `@def-classificacao-positividade`;
- produto externo: `@def-produto-externo`.

### Conteúdo novo

Definir

$$
\boldsymbol{\Sigma}
=
E
\left[
(
\boldsymbol{\mathsf X}
-
\boldsymbol{\mu}
)
(
\boldsymbol{\mathsf X}
-
\boldsymbol{\mu}
)^\top
\right].
$$

### Resultado a demonstrar no Capítulo 8

$$
\boldsymbol{\Sigma}
=
\boldsymbol{\Sigma}^\top
$$

e

$$
\boldsymbol{\Sigma}
\succeq
\mathbf0.
$$

A positividade deve ser interpretada por

$$
\mathbf a^\top
\boldsymbol{\Sigma}
\mathbf a
=
\operatorname{Var}
(
\mathbf a^\top
\boldsymbol{\mathsf X}
).
$$

A classificação de positividade em si não deve ser redefinida.

---

## Variância de combinações lineares

### Conhecimento prévio

- combinação linear: `@def-combinacao-linear`;
- forma quadrática: `@def-forma-quadratica`.

### Conteúdo novo

Demonstrar

$$
\operatorname{Var}
(
\mathbf a^\top
\boldsymbol{\mathsf X}
)
=
\mathbf a^\top
\boldsymbol{\Sigma}
\mathbf a.
$$

### Conexão futura

Esse resultado será a ponte direta para ACP.

---

## Estrutura espectral da covariância

### Conhecimento prévio

- `@thm-teorema-espectral`;
- `@thm-positividade-autovalores`;
- `@def-quociente-rayleigh`;
- `@thm-extremos-quociente-rayleigh`.

### Conteúdo novo

Interpretar os autovalores como variâncias em direções principais da distribuição.

---

## Distância de Mahalanobis

### Conhecimento prévio

- `@def-produto-interno-induzido`;
- `@def-norma-induzida`;
- `@def-forma-quadratica`;
- `@def-matriz-inversa`.

### Conteúdo novo

Definir

$$
d_M
(
\mathbf x,
\boldsymbol{\mu}
)
=
\sqrt{
(
\mathbf x-\boldsymbol{\mu}
)^\top
\boldsymbol{\Sigma}^{-1}
(
\mathbf x-\boldsymbol{\mu}
)
}.
$$

O conteúdo novo é a interpretação estatística da matriz

$$
\boldsymbol{\Sigma}^{-1}
$$

como matriz que define a geometria de Mahalanobis.

---

## Matriz de correlações

### Conhecimento prévio

- escalonamento e padronização do Capítulo 4;
- matrizes diagonais: `@def-matriz-diagonal`.

### Conteúdo novo

Construir

$$
\mathbf R
=
\mathbf D^{-1/2}
\boldsymbol{\Sigma}
\mathbf D^{-1/2},
$$

com a notação escolhida no capítulo para a matriz das variâncias marginais.

Deve-se evitar repetir a discussão geométrica geral de escalonamento.

---

# Matriz de dependências para o Capítulo 9

## Definição da normal multivariada

### Conhecimento prévio

- vetor aleatório;
- combinações lineares;
- transformações afins.

### Conteúdo novo

Caracterização pela normalidade de todas as combinações lineares.

---

## Construção pela normal padrão

### Conhecimento prévio

- `@def-transformacao-afim`;
- `@def-raiz-quadrada-principal`;
- decomposição espectral.

### Conteúdo novo

Construir

$$
\boldsymbol{\mathsf X}
=
\boldsymbol{\mu}
+
\mathbf A
\boldsymbol{\mathsf Z},
$$

com

$$
\mathbf A\mathbf A^\top
=
\boldsymbol{\Sigma}.
$$

---

## Densidade normal multivariada

### Conhecimento prévio

- `@def-densidade-conjunta`;
- `@def-determinante`;
- `@def-matriz-inversa`;
- `@def-forma-quadratica`.

### Conteúdo novo

A densidade específica da normal multivariada.

Não redefinir:

- determinante;
- matriz inversa;
- forma quadrática.

---

## Elipsoides de densidade

### Conhecimento prévio

- formas quadráticas;
- decomposição espectral;
- superfícies de nível;
- raiz de matriz.

### Conteúdo novo

Interpretar estatisticamente

$$
(
\mathbf x-\boldsymbol{\mu}
)^\top
\boldsymbol{\Sigma}^{-1}
(
\mathbf x-\boldsymbol{\mu}
)
=
c.
$$

---

## Distribuições condicionais

### Conhecimento prévio

- matriz particionada: `@def-matriz-particionada`;
- complemento de Schur: `@def-complemento-schur`;
- inversa por blocos: `@thm-inversa-complemento-schur`.

### Conteúdo novo

Derivar média e covariância condicionais da normal particionada.

---

# Matriz de dependências para o Capítulo 10

## Média amostral

### Conhecimento prévio

- matriz de dados;
- combinação linear;
- centralização.

### Conteúdo novo

- estimador;
- esperança;
- covariância;
- distribuição amostral.

---

## Matriz de covariâncias amostral

### Conhecimento prévio

- centralização;
- matriz de Gram;
- `@prp-svd-covariancia-amostral`.

### Conteúdo novo

Justificar estatisticamente

$$
\mathbf S
=
\frac{1}{n-1}
\mathbf X_c^\top
\mathbf X_c.
$$

Não repetir a relação espectral com a SVD além do necessário.

---

## Máxima verossimilhança

### Conhecimento prévio

- `@def-vetor-escore`;
- `@def-informacao-observada`;
- `@def-informacao-fisher`;
- `@prp-diferencial-inversa`;
- `@prp-diferencial-logdet`;
- `@prp-minimizador-criterio-covariancia`.

### Conteúdo novo

- verossimilhança como função da amostra;
- propriedades estatísticas dos estimadores;
- condições de regularidade;
- interpretação probabilística.

---

# Matriz de dependências para o Capítulo 11

## Modelo

$$
\mathbf Y
=
\mathbf X\mathbf B
+
\mathbf E.
$$

### Conhecimento prévio

- produtos matriciais;
- espaços coluna;
- projeções;
- mínimos quadrados;
- pseudoinversa;
- cálculo matricial.

---

## Estimação de $\mathbf B$

### Conhecimento prévio

- `@def-equacoes-normais`;
- `@prp-equacoes-normais-calculo`;
- `@prp-convexidade-minimos-quadrados-matriciais`.

### Resultado a reutilizar

$$
\widehat{\mathbf B}
=
(
\mathbf X^\top
\mathbf X
)^{-1}
\mathbf X^\top
\mathbf Y
$$

quando $\mathbf X$ possui posto completo de colunas.

O capítulo deve concentrar-se na interpretação estatística e não repetir toda a derivação geométrica e diferencial.

---

## Valores ajustados e resíduos

### Conhecimento prévio

- matriz de projeção;
- projetor residual.

Usar:

- `@prp-projecao-espaco-coluna`;
- `@def-matriz-projecao-residual`.

### Estrutura

$$
\widehat{\mathbf Y}
=
\mathbf P_X
\mathbf Y,
$$

$$
\mathbf E
=
(
\mathbf I-\mathbf P_X
)
\mathbf Y.
$$

---

## Vetorização

### Conhecimento prévio

- `@def-operador-vec`;
- `@def-produto-kronecker`;
- `@prp-identidade-vec`.

### Estrutura

$$
\operatorname{vec}
(
\mathbf Y
)
=
(
\mathbf I
\otimes
\mathbf X
)
\operatorname{vec}
(
\mathbf B
)
+
\operatorname{vec}
(
\mathbf E
).
$$

---

# Matriz de dependências para o Capítulo 12

## Aproximações locais

Usar:

- `@thm-taylor-segunda-ordem`;
- `@def-jacobiana`;
- `@def-hessiana`.

## Verossimilhança

Usar:

- `@def-vetor-escore`;
- `@def-informacao-observada`;
- `@def-informacao-fisher`.

## Transformações assintóticas

Usar:

- `@thm-metodo-delta`.

## Conteúdo novo

O Capítulo 12 deverá desenvolver:

- distribuições limites;
- testes multivariados;
- regiões de confiança;
- estatísticas pivôs;
- resultados exatos e assintóticos.

---

# Regra para redação das retomadas

## Retomada curta

Quando a definição matemática já existir:

```markdown
A forma quadrática utilizada nesta seção foi definida em @def-forma-quadratica.
```

## Retomada com interpretação nova

Quando o mesmo objeto ganha significado estatístico:

```markdown
A norma induzida foi definida em @def-norma-induzida. Nesta seção, a matriz que define essa geometria será construída a partir da matriz de covariâncias, conduzindo à distância de Mahalanobis.
```

## Retomada de resultado formal

Quando o novo desenvolvimento depende diretamente de um teorema anterior:

```markdown
Como $\boldsymbol{\Sigma}$ é simétrica, o @thm-teorema-espectral garante a existência de uma base ortonormal de autovetores.
```

## Evitar

Não repetir blocos como:

```markdown
Uma matriz é positiva definida quando...
```

se `@def-classificacao-positividade` já fornece exatamente essa definição.

---

# Checklist de revisão de cada seção dos Módulos II e III

Antes de considerar uma seção concluída, verificar:

- [ ] O objeto matemático já foi definido no Módulo I?
- [ ] Existe um identificador `@def-` que possa ser utilizado?
- [ ] Existe uma proposição ou teorema já demonstrado?
- [ ] A seção está repetindo uma demonstração já feita?
- [ ] O que está sendo acrescentado é realmente probabilístico, inferencial ou específico da técnica?
- [ ] A notação permanece compatível com o Módulo I?
- [ ] As dimensões das matrizes e vetores estão explicitadas quando necessárias?
- [ ] As condições de invertibilidade, posto ou positividade estão declaradas?
- [ ] Uma matriz inversa está sendo usada onde poderia ser necessária uma pseudoinversa?
- [ ] A diferença entre objeto populacional e amostral está clara?
- [ ] A diferença entre transformação de variáveis e transformação de coordenadas está preservada?
- [ ] A centralização está sendo usada consistentemente?
- [ ] A interpretação estatística nova foi explicitada, em vez de apenas repetir a álgebra?
- [ ] Há uma referência cruzada para o resultado do Módulo I quando pertinente?

---

# Regra para objetos populacionais e amostrais

Nos Módulos II e III, deve-se preservar claramente a distinção entre:

| Nível | Exemplo |
|:---|:---|
| Vetor aleatório | $\boldsymbol{\mathsf X}$ |
| Realização | $\mathbf x$ |
| Matriz de covariâncias populacional | $\boldsymbol{\Sigma}$ |
| Matriz de covariâncias amostral | $\mathbf S$ |
| Parâmetro | $\boldsymbol{\theta}$ |
| Estimador | $\widehat{\boldsymbol{\theta}}$ |
| Quantidade populacional | objeto sem chapéu |
| Quantidade estimada | objeto com chapéu quando apropriado |

Essa distinção será especialmente importante nos capítulos 8 a 12 e na formulação populacional e amostral das técnicas do Módulo III.

---

# Regra para condições de existência

Sempre que aparecerem objetos como

$$
\mathbf A^{-1},
$$

$$
\boldsymbol{\Sigma}^{-1},
$$

$$
\mathbf B^{-1/2},
$$

ou

$$
(
\mathbf X^\top
\mathbf X
)^{-1},
$$

deve-se declarar explicitamente a condição que garante sua existência.

Quando a singularidade fizer parte natural do problema, considerar resultados já disponíveis sobre:

- posto;
- espaços nulos;
- SVD;
- pseudoinversa.

Em particular, não substituir automaticamente uma situação singular por uma hipótese artificial de invertibilidade se a formulação com pseudoinversa for estatisticamente relevante.

---

# Regra para geometria e estatística

O Módulo I desenvolveu geometrias abstratas.

Os módulos seguintes devem mostrar a interpretação estatística dessas geometrias.

| Estrutura matemática do Módulo I | Interpretação posterior |
|:---|:---|
| Produto interno | associação, projeção e ortogonalidade |
| Norma euclidiana | magnitude |
| Norma induzida | magnitude segundo uma estrutura de ponderação |
| Forma quadrática | variância direcional ou distância quadrática |
| Matriz positiva definida | geometria não degenerada |
| Autovetor | direção privilegiada |
| Autovalor | magnitude associada a uma direção |
| Projeção | representação ou ajuste |
| SVD | decomposição das direções de linhas e colunas |
| Pseudoinversa | solução generalizada de mínimos quadrados |
| Jacobiana | propagação local de transformações |
| Hessiana | curvatura |
| Determinante | alteração de volume e medida de dispersão |
| Log-determinante | componente de critérios envolvendo matrizes positivas |
| Produto de Kronecker | representação vetorial de estruturas matriciais |

---

# Regra para os aprofundamentos do Módulo I

Os conteúdos classificados como aprofundamento permanecem disponíveis para uso posterior, mas não precisam ser retomados integralmente.

Exemplos:

- complemento de Schur;
- autovalores generalizados;
- condicionamento;
- posto numérico;
- pseudoinversa truncada;
- `vec`;
- Kronecker;
- `vech`;
- matriz de duplicação;
- matriz de eliminação;
- diferenciação sob o sinal de integração.

Quando forem necessários, deve-se utilizar a referência existente e recuperar apenas a parcela necessária ao problema estatístico.

---

# Síntese das principais cadeias de dependência

## Covariâncias

$$
\text{vetores}
\longrightarrow
\text{produto externo}
\longrightarrow
\text{esperança}
\longrightarrow
\boldsymbol{\Sigma}
\longrightarrow
\text{forma quadrática}
\longrightarrow
\text{geometria}
$$

---

## Normal multivariada

$$
\text{densidade}
+
\text{forma quadrática}
+
\text{determinante}
+
\text{inversa}
+
\text{transformação afim}
\longrightarrow
N_p
(
\boldsymbol{\mu},
\boldsymbol{\Sigma}
)
$$

---

## Regressão multivariada

$$
\text{espaço coluna}
\longrightarrow
\text{projeção}
\longrightarrow
\text{equações normais}
\longrightarrow
\text{mínimos quadrados}
\longrightarrow
\mathbf Y
=
\mathbf X\mathbf B
+
\mathbf E
$$

---

## ACP

$$
\text{combinação linear}
\longrightarrow
\text{variância direcional}
\longrightarrow
\text{forma quadrática}
\longrightarrow
\text{Rayleigh}
\longrightarrow
\text{autovetor}
$$

e, simultaneamente,

$$
\text{matriz de dados centralizada}
\longrightarrow
\text{SVD}
\longrightarrow
\text{aproximação de baixo posto}.
$$

---

## Análise discriminante

$$
\text{duas formas quadráticas}
\longrightarrow
\text{Rayleigh generalizado}
\longrightarrow
\text{autovalor generalizado}
\longrightarrow
\text{direção discriminante}.
$$

---

## Correlação canônica

$$
\text{dois blocos de variáveis}
\longrightarrow
\text{matrizes de covariância em blocos}
\longrightarrow
\text{branqueamento}
\longrightarrow
\text{problema espectral ou SVD}.
$$

---

## MANOVA

$$
\text{modelo linear}
\longrightarrow
\text{projeções}
\longrightarrow
\text{matrizes de produtos cruzados}
\longrightarrow
\text{hipótese versus erro}
\longrightarrow
\text{autovalores generalizados}.
$$

---

# Princípio final de utilização do Módulo I

O Módulo I deve ser tratado, a partir daqui, como a **biblioteca matemática interna do livro**.

Os Módulos II e III não devem reconstruir essa biblioteca. Devem utilizá-la.

Assim:

- uma forma quadrática continua sendo a forma quadrática já definida;
- uma matriz positiva continua utilizando a classificação já estabelecida;
- uma projeção continua utilizando a geometria já demonstrada;
- uma SVD continua obedecendo aos resultados já desenvolvidos;
- uma Jacobiana continua seguindo a convenção já fixada;
- uma pseudoinversa continua sendo a pseudoinversa já caracterizada.

A novidade dos módulos seguintes será progressivamente:

$$
\text{objeto matemático}
\longrightarrow
\text{objeto probabilístico}
\longrightarrow
\text{objeto inferencial}
\longrightarrow
\text{técnica multivariada}.
$$

Essa regra deve orientar a escrita, a revisão, as referências cruzadas e a decisão sobre quais demonstrações devem ou não ser repetidas.

# Catálogo de elementos didáticos e pontos de remissão do Módulo I

Este catálogo complementa o inventário conceitual e o índice de definições, proposições e teoremas.

Seu objetivo é registrar os **exemplos, figuras, tabelas e exercícios** que podem ser utilizados como pontos de remissão nos Módulos II e III.

A regra geral é:

- uma **definição** deve ser referenciada quando o objeto matemático já estiver formalizado;
- uma **proposição ou teorema** deve ser referenciado quando o resultado matemático já tiver sido estabelecido;
- um **exemplo** pode ser retomado quando o novo conteúdo for uma extensão estatística direta daquele problema;
- uma **figura** pode ser mencionada quando a interpretação geométrica já tiver sido construída;
- uma **tabela** pode ser utilizada como síntese de consulta, evitando nova enumeração das mesmas propriedades;
- um **exercício** pode ser retomado quando o estudante já tiver realizado anteriormente a construção matemática necessária à nova técnica.

---

# Capítulo 1 — Exemplos, figuras e tabelas de referência

## Exemplos

| Conteúdo | Identificador | Possível reutilização |
|:---|:---|:---|
| Vetor observado | `@exm-vetor-observado` | Vetores aleatórios e realizações no Módulo II |
| Observação em $\mathbb R^2$ | `@exm-observacao-r2` | Geometria de dados multivariados |
| Matriz de dados | `@exm-matriz-dados` | Estatísticas amostrais e modelos |
| Soma e subtração | `@exm-soma-subtracao` | Diferenças entre observações |
| Multiplicação por escalar | `@exm-multiplicacao-escalar` | Mudanças de escala |
| Combinação de vetores | `@exm-combinacao-vetores` | Combinações lineares |
| Combinação de variáveis | `@exm-combinacao-variaveis` | ACP, discriminante e correlação canônica |
| Produto interno | `@exm-produto-interno` | Associação geométrica e projeções |
| Norma euclidiana | `@exm-norma-euclidiana` | Distâncias e magnitude |
| Distância euclidiana | `@exm-distancia-euclidiana` | Agrupamentos e MDS |
| Produto externo | `@exm-produto-externo` | Covariâncias e SSCP |

## Figuras

| Conteúdo | Identificador | Possível reutilização |
|:---|:---|:---|
| Dimensões de $\mathbb R^p$ | `@fig-dimensoes-rp` | Introdução à alta dimensão |
| Observação como vetor | `@fig-vetor-observacao-r2` | Representação geométrica |
| Soma de vetores | `@fig-soma-vetores` | Combinações lineares |
| Norma de um vetor | `@fig-norma-vetor` | Geometria euclidiana |
| Produto interno | `@fig-produto-interno` | Ângulo e ortogonalidade |
| Distância euclidiana | `@fig-distancia-euclidiana` | Proximidade entre observações |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Convenções de notação | `@tbl-notacao-capitulo1` |
| Espaço das observações e espaço das variáveis | `@tbl-espacos-observacoes-variaveis` |

## Pontos de remissão prioritários

### Quando surgir uma combinação linear

Usar:

`@def-combinacao-linear`.

### Quando surgir a geometria euclidiana

Usar:

- `@def-produto-interno-euclidiano`;
- `@def-norma-euclidiana`;
- `@def-ortogonalidade`;
- `@def-distancia-euclidiana`.

### Quando surgir uma matriz formada por produtos externos

Usar:

`@def-produto-externo`.

Esse será particularmente importante na definição da matriz de covariâncias.

---

# Capítulo 2 — Exemplos, figuras e tabelas de referência

## Exemplos

| Conteúdo | Identificador |
|:---|:---|
| Soma e subtração de matrizes | `@exm-soma-subtracao-matrizes` |
| Multiplicação por escalar | `@exm-multiplicacao-matriz-escalar` |
| Produto matriz-vetor | `@exm-produto-matriz-vetor` |
| Produto matricial | `@exm-produto-matricial` |
| Produtos em matriz de dados | `@exm-produtos-matriz-dados` |
| Não comutatividade | `@exm-nao-comutatividade` |
| Transposta | `@exm-transposta-matriz` |
| Transposta de produto | `@exm-transposta-produto` |
| Matriz simétrica | `@exm-matriz-simetrica` |
| Decomposição simétrica | `@exm-decomposicao-simetrica` |
| Traço | `@exm-traco` |
| Ciclicidade do traço | `@exm-propriedade-ciclica-traco` |
| Matriz identidade | `@exm-matriz-identidade` |
| Matriz inversa | `@exm-matriz-inversa` |
| Determinante de ordem dois | `@exm-determinante-ordem-dois` |
| Determinante de ordem três | `@exm-determinante-ordem-tres` |
| Determinante e invertibilidade | `@exm-determinante-invertibilidade` |
| Posto | `@exm-posto-matrizes` |
| Sistema com solução única | `@exm-sistema-solucao-unica` |
| Sistemas sem unicidade | `@exm-sistemas-infinitas-sem-solucao` |
| Matriz ortogonal | `@exm-matriz-ortogonal` |
| Produto por matriz diagonal | `@exm-produto-matriz-diagonal` |
| Matriz triangular | `@exm-matriz-triangular` |
| Substituição regressiva | `@exm-substituicao-regressiva` |
| Forma bilinear | `@exm-forma-bilinear` |
| Forma quadrática | `@exm-forma-quadratica` |
| Positividade | `@exm-positividade-matrizes` |
| Matriz particionada | `@exm-matriz-particionada` |
| Produto por blocos | `@exm-produto-blocos` |
| Complemento de Schur | `@exm-complemento-schur` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Interpretação geométrica do determinante | `@fig-interpretacao-determinante` |
| Geometria das formas quadráticas | `@fig-formas-quadraticas` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Classificação dos sistemas lineares | `@tbl-classificacao-sistemas` |
| Casos da pseudoinversa | `@tbl-casos-pseudoinversa` |
| Síntese da álgebra matricial | `@tbl-sintese-algebra-matricial` |

## Pontos de remissão prioritários

### Matriz de covariâncias

Usar:

- `@def-matriz-simetrica`;
- `@def-classificacao-positividade`.

### Densidade normal multivariada

Usar:

- `@def-determinante`;
- `@def-matriz-inversa`;
- `@def-forma-quadratica`.

### Normal particionada e condicionais

Usar:

- `@def-matriz-particionada`;
- `@def-complemento-schur`;
- `@thm-inversa-complemento-schur`;
- `@thm-positividade-complemento-schur`.

### Critérios envolvendo matrizes

Usar:

- `@def-traco`;
- `@prp-propriedades-basicas-traco`;
- `@prp-traco-produtos-matriciais`.

---

# Capítulo 3 — Exemplos, figuras e tabelas de referência

## Exemplos

| Conteúdo | Identificador |
|:---|:---|
| Reta como subespaço | `@exm-reta-subespaco` |
| Reta que não é subespaço | `@exm-reta-nao-subespaco` |
| Subespaço gerado por uma direção | `@exm-subespaco-gerado-reta` |
| Subespaço gerado por um plano | `@exm-subespaco-gerado-plano` |
| Vetores dependentes | `@exm-vetores-dependentes` |
| Vetores independentes | `@exm-vetores-independentes` |
| Base de $\mathbb R^2$ | `@exm-base-r2` |
| Diferentes bases | `@exm-bases-diferentes` |
| Coordenadas em uma base | `@exm-coordenadas-base` |
| Dimensão de subespaços | `@exm-dimensao-subespacos` |
| Espaço nulo | `@exm-espaco-nulo` |
| Espaço coluna | `@exm-espaco-coluna` |
| Espaço linha | `@exm-espaco-linha` |
| Espaço nulo à esquerda | `@exm-espaco-nulo-esquerda` |
| Posto--Nulidade | `@exm-posto-nulidade` |
| Produto interno induzido | `@exm-produto-interno-induzido` |
| Norma e distância induzidas | `@exm-norma-distancia-induzidas` |
| Matriz de Gram | `@exm-matriz-gram` |
| Gram de base ortogonal | `@exm-gram-base-ortogonal` |
| Complemento ortogonal | `@exm-complemento-ortogonal` |
| Projeção sobre direção | `@exm-projecao-direcao` |
| Projeção por base ortonormal | `@exm-projecao-base-ortonormal` |
| Projeção sobre espaço coluna | `@exm-projecao-espaco-coluna` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Subespaço | `@fig-subespaco` |
| Independência linear | `@fig-independencia-linear` |
| Diferentes bases | `@fig-bases-diferentes` |
| Geometrias induzidas | `@fig-geometrias-induzidas` |
| Projeção ortogonal | `@fig-projecao-ortogonal` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Quatro espaços fundamentais | `@tbl-espacos-fundamentais` |
| Produtos de Gram | `@tbl-produtos-gram` |

## Pontos de remissão prioritários

### Regressão e modelo linear

Usar:

- `@def-espaco-coluna`;
- `@thm-ortogonalidade-espacos-fundamentais`;
- `@prp-projecao-espaco-coluna`;
- `@def-matriz-projecao-residual`;
- `@thm-melhor-aproximacao`;
- `@def-equacoes-normais`.

### Mahalanobis

Usar:

- `@def-produto-interno-induzido`;
- `@def-norma-induzida`.

### ACP

Usar:

- bases ortonormais;
- projeções;
- melhor aproximação.

---

# Capítulo 4 — Exemplos, figuras e tabelas de referência

## Exemplos principais

| Conteúdo | Identificador |
|:---|:---|
| Transformação de matriz de dados | `@exm-transformacao-matriz-dados` |
| Composição não comutativa | `@exm-composicao-nao-comutativa` |
| Transformação em bases distintas | `@exm-transformacao-bases-distintas` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Preservação das operações lineares | `@fig-preservacao-operacoes` |
| Efeitos de diferentes transformações | `@fig-efeitos-transformacoes` |
| Composição de transformações | `@fig-composicao-transformacoes` |
| Transformações invertíveis e não invertíveis | `@fig-inversibilidade` |
| Transformações geométricas de um quadrado | `@fig-transformacoes-quadrado` |
| Transformação ortogonal e não ortogonal | `@fig-transformacoes-ortogonais` |
| Transformação ativa e mudança passiva | `@fig-ativa-passiva` |
| Transformações lineares, afins e pré-processamento | `@fig-transformacoes-afins` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Injetividade e sobrejetividade | `@tbl-injetividade-sobrejetividade` |
| Transformações geométricas | `@tbl-transformacoes-geometricas` |
| Transformações ortogonais | `@tbl-transformacoes-ortogonais` |
| Transformação ativa e passiva | `@tbl-ativa-passiva` |
| Operações lineares e afins | `@tbl-lineares-afins` |
| Síntese das transformações | `@tbl-sintese-transformacoes` |

## Pontos de remissão prioritários

### Transformação de vetores aleatórios

Referenciar `@def-transformacao-afim`.

### Transformação de matriz de dados

Referenciar `@prp-transformacao-matriz-dados`.

### Invariância de distâncias

Referenciar `@prp-centralizacao-preserva-distancias` ou `@prp-preservacao-geometria-transformacao-ortogonal`, conforme o caso.

### Redução dimensional

Recuperar:

- posto;
- núcleo;
- imagem;
- perda de informação.

---

# Capítulo 5 — Exemplos, figuras e tabelas de referência

## Exemplos

| Conteúdo | Identificador |
|:---|:---|
| Direções invariantes | `@exm-direcoes-invariantes` |
| Decomposição espectral | `@exm-decomposicao-espectral` |
| Forma quadrática espectral | `@exm-forma-quadratica-espectral` |
| Quociente de Rayleigh | `@exm-quociente-rayleigh` |
| Potência por decomposição espectral | `@exm-potencia-espectral` |
| Pseudoinversa espectral | `@exm-pseudoinversa-espectral` |
| Autovalores generalizados | `@exm-autovalores-generalizados` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Direções invariantes | `@fig-direcoes-invariantes` |
| Multiplicidades | `@fig-multiplicidades` |
| Interpretação espectral | `@fig-interpretacao-espectral` |
| Quociente de Rayleigh | `@fig-quociente-rayleigh` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Funções espectrais | `@tbl-funcoes-espectrais` |
| Síntese espectral | `@tbl-sintese-espectral` |

## Pontos de remissão prioritários

### Matriz de covariâncias

Depois de demonstrada sua simetria:

`@thm-teorema-espectral`.

### Variância direcional

Usar:

- `@def-quociente-rayleigh`;
- `@thm-extremos-quociente-rayleigh`.

### ACP

Usar:

- `@thm-autovalores-otimizacao-restrita`;
- `@thm-caracterizacao-variacional-sucessiva`.

### Discriminante, MANOVA e correlação canônica

Usar:

- `@def-autovalor-generalizado`;
- `@def-quociente-rayleigh-generalizado`;
- `@thm-extremos-rayleigh-generalizado`.

---

# Capítulo 6 — Exemplos, figuras e tabelas de referência

## Exemplos

| Conteúdo | Identificador |
|:---|:---|
| Formas da SVD retangular | `@exm-formas-svd-retangular` |
| Construção exata da SVD | `@exm-construcao-svd` |
| Geometria da SVD | `@exm-geometria-svd` |
| Núcleo não trivial | `@exm-nucleo-nao-trivial-svd` |
| Espaços fundamentais pela SVD | `@exm-espacos-fundamentais-svd` |
| Condicionamento | `@exm-condicionamento-svd` |
| SVD de matriz de dados | `@exm-svd-matriz-dados` |
| Aproximação de posto 1 | `@exm-aproximacao-posto-um` |
| Pseudoinversa e mínimos quadrados | `@exm-pseudoinversa-minimos-quadrados` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Interpretação geométrica da SVD | `@fig-geometria-svd` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| SVD completa | `@tbl-svd-completa` |
| SVD compacta | `@tbl-svd-compacta` |
| Espaços fundamentais pela SVD | `@tbl-espacos-svd` |
| Estrutura da matriz pela SVD | `@tbl-svd-estrutura-matriz` |
| Projetores pela pseudoinversa | `@tbl-projetores-pseudoinversa` |
| Síntese da SVD | `@tbl-sintese-svd` |

## Pontos de remissão prioritários

### Covariância amostral

Usar:

`@prp-svd-covariancia-amostral`.

### ACP

Usar:

- `@thm-eckart-young-mirsky`;
- `@prp-aproximacao-dados-projecao-svd`;
- `@prp-svd-covariancia-amostral`.

### Análise de Correspondência

Usar:

- `@def-produto-interno-frobenius`;
- `@def-norma-frobenius`;
- `@thm-existencia-svd`;
- `@thm-eckart-young-mirsky`.

### Sistemas singulares e regressão

Usar:

- `@def-pseudoinversa-svd`;
- `@prp-minimos-quadrados-pseudoinversa`.

---

# Capítulo 7 — Exemplos, figuras e tabelas de referência

## Exemplos de cálculo diferencial

| Conteúdo | Identificador |
|:---|:---|
| Diferencial de função linear | `@exm-diferencial-funcao-linear` |
| Diferencial da norma quadrática | `@exm-diferencial-norma-quadratica` |
| Gradiente e curva de nível | `@exm-gradiente-curva-nivel` |
| Jacobiana | `@exm-jacobiana` |
| Regra da cadeia | `@exm-regra-cadeia` |
| Hessiana de função quadrática | `@exm-hessiana-funcao-quadratica` |
| Taylor de primeira ordem | `@exm-taylor-primeira-ordem` |
| Taylor de segunda ordem | `@exm-taylor-segunda-ordem` |
| Classificação de pontos | `@exm-classificacao-pontos` |
| Newton para função quadrática | `@exm-newton-funcao-quadratica` |
| Lagrange com restrição de norma | `@exm-lagrange-restricao-norma` |

## Exemplos de mínimos quadrados e cálculo matricial

| Conteúdo | Identificador |
|:---|:---|
| Média como problema de mínimos quadrados | `@exm-media-minimos-quadrados` |
| Forma quadrática matricial | `@exm-forma-quadratica-matricial` |
| Derivada da inversa em caso simples | `@exm-derivada-inversa-escalar` |
| Traço envolvendo inversa | `@exm-traco-inversa` |

## Exemplos de vetorização

| Conteúdo | Identificador |
|:---|:---|
| Vetorização de matriz | `@exm-vetorizacao-matriz` |
| Produto de Kronecker | `@exm-produto-kronecker` |
| Identidade `vec` | `@exm-vec-produto-matricial` |
| Meia vetorização | `@exm-meia-vetorizacao` |
| Jacobiana de produto cruzado | `@exm-jacobiana-produto-cruzado` |

## Exemplos de integração e inferência

| Conteúdo | Identificador |
|:---|:---|
| Marginalização | `@exm-marginalizacao` |
| Transformação escalar de densidade | `@exm-transformacao-escalar-densidade` |
| Diferenciação da normalização | `@exm-derivacao-normalizacao` |
| Método delta para razão | `@exm-metodo-delta-razao` |
| Estimação do vetor de médias | `@exm-estimacao-vetor-medias` |
| Critério do modelo linear multivariado | `@exm-criterio-modelo-linear-multivariado` |

## Figuras

| Conteúdo | Identificador |
|:---|:---|
| Aproximação linear direcional | `@fig-aproximacao-linear-direcional` |
| Gradiente e curvas de nível | `@fig-gradiente-curvas-nivel` |
| Curvatura e Hessiana | `@fig-curvatura-hessiana` |
| Jacobiano e alteração de área | `@fig-jacobiano-alteracao-area` |

## Tabelas

| Conteúdo | Identificador |
|:---|:---|
| Tipos de funções | `@tbl-tipos-funcoes-calculo` |
| Cálculo e inferência | `@tbl-calculo-inferencia` |
| Relações entre ferramentas de cálculo | `@tbl-relacoes-calculo` |
| Identidades de cálculo vetorial e matricial | `@tbl-identidades-calculo` |

## Pontos de remissão prioritários

### Máxima verossimilhança

Usar:

- `@def-vetor-escore`;
- `@def-informacao-observada`;
- `@def-informacao-fisher`.

### Estimação de covariâncias

Usar:

- `@prp-diferencial-inversa`;
- `@prp-formula-jacobi`;
- `@prp-diferencial-logdet`;
- `@prp-minimizador-criterio-covariancia`.

### Modelo linear multivariado

Usar:

- `@prp-equacoes-normais-calculo`;
- `@prp-convexidade-minimos-quadrados-matriciais`;
- `@prp-identidade-vec`.

### Transformações de distribuições

Usar:

- `@thm-mudanca-variaveis-integral`;
- `@prp-transformacao-densidade`.

### Teoria assintótica

Usar:

- `@thm-taylor-segunda-ordem`;
- `@def-jacobiana`;
- `@thm-metodo-delta`.

---

# Exercícios de fixação — catálogo por finalidade

Os exercícios de fixação também podem funcionar como referências didáticas nos módulos seguintes.

## Representação, centralização e transformação

- `@exr-fix-m1-transformacao-dados`;
- `@exr-fix-m1-centralizacao`;
- `@exr-fix-m1-escalonamento`;
- `@exr-fix-m1-r-centralizacao`;
- `@exr-fix-m1-r-transformacao-iris`.

## Matrizes de Gram

- `@exr-fix-m1-gram`;
- `@exr-fix-m1-r-gram`.

## Projeções e mínimos quadrados

- `@exr-fix-m1-projecao-residuos`;
- `@exr-fix-m1-minimos-quadrados`;
- `@exr-fix-m1-gradiente-minimos-quadrados`;
- `@exr-fix-m1-r-minimos-quadrados`;
- `@exr-fix-m1-r-pseudoinversa`.

## Geometria induzida e complemento de Schur

- `@exr-fix-m1-norma-induzida`;
- `@exr-fix-m1-schur`.

## Direções espectrais

- `@exr-fix-m1-forma-quadratica`;
- `@exr-fix-m1-maxima-dispersao`;
- `@exr-fix-m1-direcoes-sucessivas`;
- `@exr-fix-m1-lagrange-espectral`;
- `@exr-fix-m1-r-rayleigh`;
- `@exr-fix-m1-r-projecao`.

## Autovalores generalizados e branqueamento

- `@exr-fix-m1-autovalor-generalizado`;
- `@exr-fix-m1-branqueamento`.

## SVD e aproximação de posto reduzido

- `@exr-fix-m1-svd`;
- `@exr-fix-m1-svd-truncada`;
- `@exr-fix-m1-r-svd`;
- `@exr-fix-m1-r-svd-truncada`.

## Vetorização e modelo matricial

- `@exr-fix-m1-vec-kronecker`;
- `@exr-fix-m1-r-vec`.

## Cálculo matricial

- `@exr-fix-m1-criterio-matricial`.

## Transformações e integração

- `@exr-fix-m1-transformacao-afim`;
- `@exr-fix-m1-integracao-marginais`.

## Teoria assintótica

- `@exr-fix-m1-metodo-delta`.

---

# Remissões didáticas sugeridas para o Módulo II

## Capítulo 8 — Matrizes de covariâncias e correlações

Quando for introduzida a matriz

$$
\mathbf S
=
\frac{1}{n-1}
\mathbf X_c^\top
\mathbf X_c,
$$

pode-se remeter a:

- `@prp-svd-covariancia-amostral`;
- `@exr-fix-m1-r-gram`.

Quando for apresentada a variância direcional,

$$
\mathbf a^\top
\boldsymbol{\Sigma}
\mathbf a,
$$

pode-se remeter a:

- `@exm-forma-quadratica`;
- `@exm-quociente-rayleigh`;
- `@exr-fix-m1-maxima-dispersao`.

Quando for apresentada a distância de Mahalanobis, pode-se remeter a:

- `@exm-norma-distancia-induzidas`;
- `@exr-fix-m1-norma-induzida`.

---

## Capítulo 9 — Distribuição Normal Multivariada

Quando forem apresentadas superfícies de igual densidade, pode-se remeter a:

- `@exm-forma-quadratica-espectral`;
- `@fig-interpretacao-espectral`.

Quando for utilizada transformação afim, pode-se remeter a:

- `@def-transformacao-afim`;
- `@fig-transformacoes-afins`;
- `@exr-fix-m1-transformacao-afim`.

Quando aparecer a mudança de variáveis, pode-se remeter a:

- `@thm-mudanca-variaveis-integral`;
- `@fig-jacobiano-alteracao-area`.

---

## Capítulo 10 — Estatísticas Amostrais e Estimação

Quando aparecer a máxima verossimilhança de uma matriz de covariâncias, pode-se remeter a:

- `@prp-minimizador-criterio-covariancia`;
- `@exr-fix-m1-criterio-matricial`.

Quando aparecer o método delta, remeter a:

- `@thm-metodo-delta`;
- `@exm-metodo-delta-razao`;
- `@exr-fix-m1-metodo-delta`.

---

## Capítulo 11 — Modelo Linear Multivariado

Quando forem apresentadas equações normais, pode-se remeter a:

- `@def-equacoes-normais`;
- `@prp-equacoes-normais-calculo`;
- `@exr-fix-m1-projecao-residuos`;
- `@exr-fix-m1-gradiente-minimos-quadrados`.

Quando aparecer a formulação vetorizada, pode-se remeter a:

- `@prp-identidade-vec`;
- `@exr-fix-m1-r-vec`.

Quando houver posto deficiente, pode-se remeter a:

- `@def-pseudoinversa-svd`;
- `@prp-minimos-quadrados-pseudoinversa`;
- `@exr-fix-m1-r-pseudoinversa`.

---

# Remissões didáticas sugeridas para o Módulo III

## ACP

Remissões particularmente naturais:

- `@exm-quociente-rayleigh`;
- `@exr-fix-m1-maxima-dispersao`;
- `@exr-fix-m1-direcoes-sucessivas`;
- `@exr-fix-m1-r-rayleigh`;
- `@exr-fix-m1-r-projecao`;
- `@exr-fix-m1-r-svd-truncada`.

O aluno poderá reconhecer que a nova técnica combina problemas já resolvidos anteriormente.

---

## Análise Discriminante

Remissões particularmente naturais:

- `@exm-autovalores-generalizados`;
- `@exr-fix-m1-autovalor-generalizado`;
- `@exr-fix-m1-branqueamento`.

---

## MANOVA

Remissões particularmente naturais:

- `@exr-fix-m1-projecao-residuos`;
- `@exr-fix-m1-gradiente-minimos-quadrados`;
- `@exr-fix-m1-autovalor-generalizado`.

---

## Análise de Correspondência

Remissões particularmente naturais:

- `@exm-svd-matriz-dados`;
- `@exm-aproximacao-posto-um`;
- `@exr-fix-m1-r-svd-truncada`.

---

## Correlação Canônica

Remissões particularmente naturais:

- `@exm-autovalores-generalizados`;
- `@exr-fix-m1-autovalor-generalizado`;
- `@exr-fix-m1-branqueamento`.

---

# Estado consolidado do Módulo I

A partir deste inventário, o Módulo I pode ser considerado uma base consolidada contendo cinco tipos de recursos reutilizáveis:

| Recurso | Função nos módulos seguintes |
|:---|:---|
| Definições | Fixar definitivamente os objetos matemáticos |
| Proposições e teoremas | Evitar repetir resultados já demonstrados |
| Exemplos | Recuperar construções já conhecidas pelo estudante |
| Figuras e tabelas | Recuperar interpretações geométricas e sínteses |
| Exercícios de fixação | Mostrar que determinadas operações já foram praticadas |

A elaboração dos Módulos II e III deverá consultar esses cinco níveis antes de introduzir novamente qualquer estrutura matemática.

---

# Encerramento do inventário do Módulo I

Com os blocos anteriores, ficam registrados:

1. a estrutura de cada capítulo do Módulo I;
2. o conteúdo de suas seções e subseções;
3. as definições;
4. as proposições;
5. os teoremas;
6. os principais exemplos;
7. as figuras;
8. as tabelas;
9. os exercícios de fixação;
10. os conceitos deliberadamente ainda não definidos;
11. as dependências previstas para os capítulos do Módulo II;
12. as dependências previstas para as técnicas do Módulo III;
13. as regras editoriais para evitar redundâncias;
14. os pontos de remissão didática entre os módulos.

O princípio a ser preservado é:

$$
\text{Módulo I}
\longrightarrow
\text{fundamentos matemáticos e geométricos},
$$

$$
\text{Módulo II}
\longrightarrow
\text{fundamentos probabilísticos e inferenciais},
$$

e

$$
\text{Módulo III}
\longrightarrow
\text{formulação matemática e estatística das técnicas}.
$$

A partir deste ponto, o Módulo I deve ser tratado como **base consolidada de consulta**, permanecendo inalterado até uma futura revisão específica.

Durante a elaboração dos Módulos II e III, a prioridade deve ser utilizar referências cruzadas para o Módulo I sempre que o objeto matemático já estiver definido ou o resultado necessário já tiver sido demonstrado.